<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  此 XML 已清空（2026-07-xx）：
  原 selectPage 通过 @Override BaseMapper.selectPage 方式提供 SQL，但该模式
  会导致 MyBatis-Plus 自动注入失效 → BindingException 500。
  修复方案：@InterceptorIgnore 移至 Mapper 类级别，selectPage 由 MyBatis-Plus 自动注入。
  v_finished_product_settlement 是视图，@TableName 已正确映射，auto-inject SQL 即可。
-->
<mapper namespace="com.fashion.supplychain.finance.mapper.FinishedProductSettlementMapper">
</mapper>
