-- D-170：清理工序表中的「采购/入库」残留行（与 PC 端新流程对齐）
-- 根因：新流程中采购与入库是独立动作（采购走「管理采购」，入库走仓库扫码），
--       PC 端工序列表早已移除采购/入库阶段（SampleProcessList.tsx，STAGE_ORDER 仅
--       裁剪/二次工艺/车缝/尾部），但手机端工序列表直接读 t_style_process 全量数据，
--       历史插入的采购/入库工序行（7 个款式共 11 行）仍在，导致手机端工序列表
--       显示采购/入库卡片且可"领取"，与 PC 端不一致。
-- 安全：这些工序名无任何 t_pattern_scan_record 引用（已验证 delete_flag=0 记录为 0）；
--       7 个款式（49/66/74/78/83/84/132）删除后仍保留全部真实生产工序。
-- 幂等：DELETE 精确匹配，重复执行影响 0 行。

DELETE FROM t_style_process WHERE process_name IN ('采购', '入库');
