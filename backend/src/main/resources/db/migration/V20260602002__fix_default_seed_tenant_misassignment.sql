-- ============================================================
-- 修复 V20260412001 历史 bug：系统默认种子被错填为 tenant_id=1
--
-- 背景：
--   1. V20260302c 创建 t_process_parent_mapping 表（无 tenant_id 列）
--   2. V20260412001 添加 tenant_id 列时，DEFAULT 1 导致已有 40 条系统默认
--      种子被错填为 tenant_id=1
--   3. V202607202200 补充二次工艺 12 条时也误标 tenant_id=1
--   4. 结果：租户 1 (华南制衣厂) 凭空多了 52 条"自己的"配置，
--      其他租户只有 15 条，租户间数据严重不公平
--
-- 修复：把 tenant_id=1 的 52 条全部改为 NULL，恢复"系统默认"语义。
-- 配合 V20260602001（去重）一起，数据库现有 67 条系统默认映射。
--
-- 数据影响：
--   - 租户 1 失去"专属"52 条（实际未失去：SHARED 模式 (tenant_id=1 OR tenant_id IS NULL)
--     仍能查到，行为不变）
--   - 其他租户获得完整 67 条系统默认（之前只有 15 条）
--   - 业务无影响：每条数据的 process_keyword 和 parent_node 保持不变
-- ============================================================

UPDATE t_process_parent_mapping
SET tenant_id = NULL
WHERE tenant_id = 1;
