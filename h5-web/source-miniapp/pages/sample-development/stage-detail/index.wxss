/**
 * 样衣阶段详情页样式（设计稿重构版）
 * 匹配 sample-stage-detail.html 设计稿
 * 使用 CSS 变量 + rpx 单位
 */

/* ══ 页面基础 ══ */
page {
  min-height: 100vh;
  background: var(--color-bg-page);
  padding: var(--spacing-md) 16px;
  padding-bottom: 140px;
  box-sizing: border-box;
}

/* ══ 加载状态 ══ */
.-加载状态- {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120px 0;
}
{
  width: 32px;
  height: 32px;
  border: 3px solid var(--color-border);
  border-top-color: var(--color-primary);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}
{
  to { transform: rotate(360deg); }
}
{
  margin-top: 12px;
  font-size: var(--font-size-md);
  color: var(--color-text-tertiary);
}

/* ══ ① 样衣信息卡 ══ */
.-①-样衣信息卡- {
  background: var(--color-bg-card);
  /* Apple style: removed hard border, using shadow instead */
  border-radius: var(--radius-card);
  padding: var(--spacing-lg);
  box-shadow: var(--shadow-card);
  margin-bottom: var(--card-gap);
}

/* 顶部：封面 + 信息 + 进度环 */
.顶部封面--信息--进度环 {
  display: flex;
  align-items: flex-start;
  gap: 12px;
}

/* 封面 */
.封面 {
  width: 64px;
  height: 64px;
  flex-shrink: 0;
  border-radius: var(--radius-sm);
  overflow: hidden;
  background: linear-gradient(135deg, var(--color-bg-page), var(--color-border-light));
}
{
  width: 100%;
  height: 100%;
}
{
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}
{
  font-size: 24px;
  color: var(--color-text-tertiary);
}

/* 信息区 */
.信息区 {
  flex: 1;
  min-width: 0;
}
{
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}
{
  font-size: var(--font-size-md);
  font-weight: 700;
  color: var(--color-text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
{
  flex-shrink: 0;
  height: 20px;
  line-height: 20px;
  padding: 0 8px;
  border-radius: 9999px;
  font-size: var(--font-size-xs);
  font-weight: 500;
  color: var(--color-primary);
  background: var(--color-primary-bg);
  white-space: nowrap;
}
{
  display: block;
  margin-top: 4px;
  font-size: var(--font-size-lg);
  font-weight: 600;
  color: var(--color-text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
{
  display: block;
  margin-top: 2px;
  font-size: var(--font-size-sm);
  color: var(--color-text-secondary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 进度环 */
.进度环 {
  width: 48px;
  height: 48px;
  flex-shrink: 0;
  border-radius: 50%;
  background: conic-gradient(var(--color-primary) calc(var(--p, 0) * 1%), var(--color-border) 0);
  display: flex;
  align-items: center;
  justify-content: center;
}
{
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: var(--color-bg-card);
  display: flex;
  align-items: center;
  justify-content: center;
}
{
  font-size: var(--font-size-xs);
  font-weight: 700;
  color: var(--color-text-primary);
}

/* 信息网格 */
.信息网格 {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px 16px;
  margin-top: 12px;
  font-size: var(--font-size-sm);
}
{
  display: flex;
  align-items: center;
  gap: 6px;
  min-width: 0;
}
{
  flex-shrink: 0;
  color: var(--color-text-tertiary);
}
{
  font-weight: 500;
  color: var(--color-text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 分割线 */
.分割线 {
  height: 1px;
  background: var(--color-border);
  margin: var(--spacing-md) 0;
}

/* 底部：数量 + 交期 */
.底部数量--交期 {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
{
  display: flex;
  align-items: center;
  gap: 8px;
}
{
  font-size: var(--font-size-sm);
  color: var(--color-text-tertiary);
}
{
  font-size: var(--font-size-md);
  font-weight: 700;
  color: var(--color-text-primary);
}
{
  display: flex;
  align-items: center;
  gap: 8px;
}
{
  font-size: var(--font-size-sm);
  color: var(--color-text-tertiary);
}
{
  font-size: var(--font-size-md);
  font-weight: 500;
  color: var(--color-text-primary);
}
{
  height: 20px;
  line-height: 20px;
  padding: 0 6px;
  border-radius: 9999px;
  font-size: var(--font-size-xs);
  font-weight: 500;
  white-space: nowrap;
}
{
  background: var(--color-success-bg);
  color: var(--color-success);
}
{
  background: var(--color-warning-bg);
  color: var(--color-warning);
}
{
  background: var(--color-danger-bg);
  color: var(--color-danger);
}

/* ══ ② 阶段时间轴 ══ */
.-②-阶段时间轴- {
  margin-bottom: 12px;
}
{
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 4px 8px;
}
{
  font-size: var(--font-size-base);
  font-weight: 600;
  color: var(--color-text-primary);
}
{
  font-size: var(--font-size-sm);
  color: var(--color-text-tertiary);
}

/* 时间轴容器 */
.时间轴容器 {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

/* 垂直连接线 */
.垂直连接线 {
  position: absolute;
  left: 15px;
  top: 16px;
  bottom: 16px;
  width: 2px;
  background: var(--color-border);
}

/* 阶段条目 */
.阶段条目 {
  position: relative;
  padding-left: 40px;
}

/* 状态圆 */
.状态圆 {
  position: absolute;
  left: 0;
  top: 0;
  width: 32px;
  height: 32px;
  z-index: 1;
}
{
  width: 32px;
  height: 32px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 已完成圆 */
.已完成圆 {
  background: var(--color-success);
}
{
  color: var(--color-text-white);
  font-size: var(--font-size-lg);
  font-weight: 700;
  line-height: 1;
}

/* 进行中圆 + 脉冲动画 */
.进行中圆--脉冲动画 {
  background: var(--color-primary);
  position: relative;
}
{
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  border-radius: 50%;
  background: var(--color-primary);
  opacity: 0.35;
  animation: ts-pulse 1.5s ease-out infinite;
}
{
  position: relative;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: var(--color-text-white);
}
{
  0% {
    transform: scale(1);
    opacity: 0.35;
  }
  100% {
    transform: scale(1.6);
    opacity: 0;
  }
}

/* 未开始圆 */
.未开始圆 {
  border: 2px solid var(--color-border-light);
  background: var(--color-bg-card);
}
{
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--color-border-light);
}

/* 阶段卡片 */
.阶段卡片 {
  background: var(--color-bg-card);
  /* Apple style: removed hard border, using shadow instead */
  border-radius: var(--radius-card);
  padding: var(--card-padding);
  box-shadow: var(--shadow-xs);
}
{
  border-color: var(--color-primary-border);
  box-shadow: var(--shadow-card);
}
{
  opacity: 0.6;
}

/* 卡片头部 */
.卡片头部 {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}
{
  font-size: var(--font-size-md);
  font-weight: 600;
  color: var(--color-text-primary);
}
{
  color: var(--color-text-secondary);
}

/* 状态标签 */
.状态标签 {
  height: 20px;
  line-height: 20px;
  padding: 0 8px;
  border-radius: 9999px;
  font-size: var(--font-size-xs);
  font-weight: 500;
  white-space: nowrap;
}
{
  background: var(--color-success-bg);
  color: var(--color-success);
}
{
  background: var(--color-primary-bg);
  color: var(--color-primary);
}
{
  background: var(--color-bg-page);
  color: var(--color-text-tertiary);
}

/* 卡片元信息 */
.卡片元信息 {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-top: 6px;
  font-size: var(--font-size-sm);
  color: var(--color-text-secondary);
}
{
  color: var(--color-text-tertiary);
}
{
  white-space: nowrap;
}
{
  color: var(--color-text-tertiary);
}
{
  white-space: nowrap;
}

/* 延时提示 */
.延时提示 {
  margin-top: 4px;
  font-size: var(--font-size-sm);
  font-weight: 500;
}
{
  color: var(--color-warning);
}
{
  color: var(--color-danger);
}

/* 当前阶段标记 */
.当前阶段标记 {
  display: inline-block;
  margin-top: 6px;
  height: 20px;
  line-height: 20px;
  padding: 0 8px;
  border-radius: 9999px;
  font-size: var(--font-size-xs);
  font-weight: 500;
  background: var(--color-primary-light);
  color: var(--color-primary);
}

/* 查看详情按钮 */
.查看详情按钮 {
  display: flex;
  align-items: center;
  gap: 4px;
  margin-top: 8px;
  font-size: var(--font-size-sm);
  font-weight: 500;
  color: var(--color-primary);
}
{
  font-size: var(--font-size-lg);
  line-height: 1;
}

/* ══ ③ 当前阶段详情卡 ══ */
.-③-当前阶段详情卡- {
  background: var(--color-bg-card);
  border: 1px solid var(--color-primary-border);
  border-radius: var(--radius-card);
  padding: var(--spacing-lg);
  box-shadow: var(--shadow-card);
  margin-bottom: var(--card-gap);
}

/* 头部 */
.头部 {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}
{
  display: flex;
  align-items: center;
  gap: 8px;
}
{
  width: 24px;
  height: 24px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}
{
  background: var(--color-success);
}
{
  background: var(--color-primary);
}
{
  background: var(--color-border-light);
}
{
  font-size: var(--font-size-sm);
  font-weight: 700;
  color: var(--color-text-white);
}
{
  font-size: var(--font-size-base);
  font-weight: 600;
  color: var(--color-text-primary);
}

/* 状态标签（复用 ts-card-tag） */
.状态标签复用-ts-card-tag {
  height: 20px;
  line-height: 20px;
  padding: 0 8px;
  border-radius: 9999px;
  font-size: var(--font-size-xs);
  font-weight: 500;
  white-space: nowrap;
}
{
  background: var(--color-success-bg);
  color: var(--color-success);
}
{
  background: var(--color-primary-bg);
  color: var(--color-primary);
}
{
  background: var(--color-bg-page);
  color: var(--color-text-tertiary);
}

/* 详情网格 */
.详情网格 {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px 16px;
  margin-top: 12px;
  font-size: var(--font-size-sm);
}
{
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-width: 0;
}
{
  flex-shrink: 0;
  color: var(--color-text-tertiary);
}
{
  font-weight: 500;
  color: var(--color-text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 分割线 */
.分割线 {
  height: 1px;
  background: var(--color-border);
  margin: var(--spacing-md) 0;
}

/* 时间信息 */
.时间信息 {
  display: flex;
  flex-direction: column;
  gap: 6px;
  font-size: var(--font-size-sm);
}
{
  display: flex;
  align-items: center;
  gap: 8px;
}
{
  width: 14px;
  height: 14px;
  flex-shrink: 0;
  display: inline-block;
}
{
  flex-shrink: 0;
  color: var(--color-text-tertiary);
}
{
  margin-left: auto;
  font-weight: 500;
  color: var(--color-text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 现场照片 */
.现场照片 {
  margin-top: 12px;
}
{
  display: flex;
  align-items: center;
  justify-content: space-between;
}
{
  font-size: var(--font-size-sm);
  font-weight: 500;
  color: var(--color-text-secondary);
}
{
  font-size: var(--font-size-xs);
  color: var(--color-text-tertiary);
}
{
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
  margin-top: 8px;
}
{
  width: 100%;
  aspect-ratio: 1;
  border-radius: var(--radius-sm);
  /* Apple: removed hard border */
}

/* 操作按钮 */
.操作按钮 {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 12px;
}

/* 已完成提示 */
.已完成提示 {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  margin-top: 12px;
  padding: var(--spacing-md);
  background: var(--color-success-bg);
  border-radius: var(--radius-sm);
}
{
  width: 20px;
  height: 20px;
  line-height: 20px;
  text-align: center;
  border-radius: 50%;
  background: var(--color-success);
  color: var(--color-text-white);
  font-size: var(--font-size-sm);
  font-weight: 700;
}
{
  font-size: var(--font-size-sm);
  font-weight: 500;
  color: var(--color-success);
}
{
  flex: 1;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-sm);
  font-size: var(--font-size-sm);
  font-weight: 500;
  white-space: nowrap;
}
{
  background: transparent;
  color: var(--color-primary);
  border: 1.5px solid var(--color-primary);
}
{
  /* Apple: removed hard border */
  background: var(--color-bg-card);
  color: var(--color-text-primary);
}
{
  font-size: var(--font-size-sm);
}

/* ══ 通用：段落标题 ══ */
.-通用段落标题- {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--spacing-md) 4px 8px;
}
{
  font-size: var(--font-size-md);
  font-weight: 600;
  color: var(--color-text-primary);
}
{
  font-size: var(--font-size-sm);
  color: var(--color-text-tertiary);
}

/* ══ 通用：数据卡片 ══ */
.-通用数据卡片- {
  background: var(--color-bg-card);
  /* Apple style: removed hard border, using shadow instead */
  border-radius: var(--radius-sm);
  padding: var(--card-padding);
  margin-bottom: 8px;
  box-shadow: var(--shadow-xs);
}
{
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}
{
  display: flex;
  align-items: center;
  gap: 8px;
  min-width: 0;
}
{
  font-size: var(--font-size-md);
  font-weight: 600;
  color: var(--color-text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
{
  height: 20px;
  line-height: 20px;
  padding: 0 8px;
  border-radius: 9999px;
  font-size: var(--font-size-xs);
  font-weight: 500;
  background: var(--color-bg-page);
  color: var(--color-text-tertiary);
}
{
  font-size: var(--font-size-md);
  font-weight: 600;
  color: var(--color-primary);
  white-space: nowrap;
}
{
  margin-top: 8px;
}
{
  display: flex;
  align-items: flex-start;
  gap: 8px;
  padding: var(--spacing-xs) 0;
  font-size: var(--font-size-sm);
}
{
  align-items: flex-start;
}
{
  flex-shrink: 0;
  width: 64px;
  color: var(--color-text-tertiary);
}
{
  flex: 1;
  color: var(--color-text-primary);
  word-break: break-all;
}
{
  display: block;
  line-height: 1.6;
  color: var(--color-text-primary);
}
{
  font-size: var(--font-size-sm);
  color: var(--color-text-secondary);
}

/* 工序状态标签 */
.工序状态标签 {
  height: 20px;
  line-height: 20px;
  padding: 0 8px;
  border-radius: 9999px;
  font-size: var(--font-size-xs);
  font-weight: 500;
  white-space: nowrap;
}
{
  background: var(--color-success-bg);
  color: var(--color-success);
}
{
  background: var(--color-primary-bg);
  color: var(--color-primary);
}
{
  background: var(--color-bg-page);
  color: var(--color-text-tertiary);
}

/* 工序撤回按钮 */
.工序撤回按钮 {
  margin-top: 8px;
  padding-top: 8px;
  border-top: 1px solid var(--color-border-light);
}
{
  color: var(--color-danger);
  font-size: var(--font-size-sm);
  font-weight: 500;
}

/* ══ BOM 汇总栏 ══ */
.-bom-汇总栏- {
  display: flex;
  align-items: center;
  background: var(--color-bg-card);
  /* Apple style: removed hard border */
  border-radius: var(--radius-sm);
  padding: var(--spacing-md);
  margin-bottom: 8px;
  box-shadow: var(--shadow-xs);
}
{
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
}
{
  font-size: var(--font-size-xs);
  color: var(--color-text-tertiary);
}
{
  font-size: var(--font-size-md);
  font-weight: 600;
  color: var(--color-text-primary);
}
{
  color: var(--color-primary);
}
{
  width: 1px;
  height: 24px;
  background: var(--color-border);
}

/* 分组标题 */
.分组标题 {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--spacing-sm) 4px;
}
{
  font-size: var(--font-size-sm);
  font-weight: 600;
  color: var(--color-text-secondary);
}
{
  font-size: var(--font-size-xs);
  color: var(--color-text-tertiary);
}

/* ══ 尺码表 ══ */
.-尺码表- {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 8px;
}
{
  height: 24px;
  line-height: 24px;
  padding: 0 10px;
  border-radius: 9999px;
  font-size: var(--font-size-sm);
  font-weight: 500;
  background: var(--color-primary-light);
  color: var(--color-primary);
}
{
  overflow-x: auto;
  /* Apple: removed hard border */
  border-radius: var(--radius-xs);
  margin-top: 8px;
}
{
  display: flex;
  background: var(--color-border-light);
  border-bottom: 1px solid var(--color-border);
}
{
  display: flex;
  border-bottom: 1px solid var(--color-border);
}
{
  border-bottom: none;
}
.sth-cell,
{
  padding: var(--spacing-sm) 6px;
  font-size: var(--font-size-sm);
  text-align: center;
  word-break: break-all;
  border-right: 1px solid var(--color-border);
}
.sth-cell:last-child,
{
  border-right: none;
}
{
  font-weight: 600;
  color: var(--color-text-secondary);
}
{
  color: var(--color-text-primary);
}
.sth-part,
{
  min-width: 60px;
  text-align: left;
}
.sth-method,
{
  min-width: 80px;
}
.sth-tol,
{
  min-width: 50px;
}
.sth-base,
{
  min-width: 50px;
}
.sth-size,
{
  min-width: 50px;
}
{
  overflow: hidden;
}
{
  margin-top: 0;
  border: none;
  border-radius: 0;
}

/* ══ 二次工艺图片 ══ */
.-二次工艺图片- {
  margin-top: 8px;
}
{
  width: 100%;
  height: 120px;
  border-radius: var(--radius-sm);
  /* Apple: removed hard border */
}

/* ══ 生产制单封面 ══ */
.-生产制单封面- {
  display: flex;
  flex-direction: column;
  gap: 12px;
}
{
  width: 100%;
}
{
  width: 100%;
  height: 180px;
  border-radius: var(--radius-sm);
  /* Apple: removed hard border */
}

/* 审核状态 */
.审核状态 {
  font-weight: 600;
}
{
  color: var(--color-success);
}
{
  color: var(--color-warning);
}
{
  color: var(--color-danger);
}

/* ══ 空状态 ══ */
.-空状态- {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 32px 16px;
}
{
  padding: var(--spacing-lg);
}
{
  font-size: var(--font-size-sm);
  color: var(--color-text-tertiary);
}

/* ══ 附件区 ══ */
.-附件区- {
  margin-top: 12px;
}
{
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  height: 44px;
  background: var(--color-bg-card);
  border: 1px dashed var(--color-primary-border);
  border-radius: var(--radius-sm);
  margin-bottom: 8px;
}
{
  font-size: var(--font-size-sm);
  color: var(--color-primary);
  font-weight: 500;
}
{
  font-size: var(--font-size-xl);
  color: var(--color-primary);
  font-weight: 700;
}
{
  display: flex;
  align-items: center;
  gap: 12px;
  background: var(--color-bg-card);
  /* Apple style: removed hard border, using shadow instead */
  border-radius: var(--radius-sm);
  padding: var(--card-padding);
  margin-bottom: 8px;
  box-shadow: var(--shadow-xs);
}
{
  width: 36px;
  height: 36px;
  border-radius: var(--radius-xs);
  background: var(--color-primary-light);
  flex-shrink: 0;
}
{
  background: var(--color-danger-bg);
}
{
  background: var(--color-primary-bg);
}
{
  background: var(--color-success-bg);
}
{
  flex: 1;
  min-width: 0;
}
{
  font-size: var(--font-size-sm);
  font-weight: 500;
  color: var(--color-text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  display: block;
}
{
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 2px;
}
.att-uploader,
.att-time,
{
  font-size: var(--font-size-xs);
  color: var(--color-text-tertiary);
}
{
  flex-shrink: 0;
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
}
{
  font-size: var(--font-size-xl);
  color: var(--color-text-tertiary);
}

/* ══ 备注日志 ══ */
.-备注日志- {
  margin-top: 12px;
}
{
  background: var(--color-bg-card);
  /* Apple: removed hard border */
  border-radius: var(--radius-sm);
  padding: var(--spacing-md);
  margin-bottom: 8px;
}
{
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 8px;
}
{
  flex: 1;
  height: 36px;
  padding: 0 12px;
  /* Apple: removed hard border */
  border-radius: var(--radius-xs);
  font-size: var(--font-size-sm);
  color: var(--color-text-primary);
}
{
  height: 36px;
  padding: 0 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-xs);
  background: transparent;
  border: 1.5px solid var(--color-primary);
  font-size: var(--font-size-sm);
  font-weight: 500;
  color: var(--color-primary);
  white-space: nowrap;
}
{
  opacity: 0.6;
}
{
  width: 100%;
  min-height: 60px;
  padding: var(--spacing-sm) 12px;
  /* Apple: removed hard border */
  border-radius: var(--radius-xs);
  font-size: var(--font-size-sm);
  color: var(--color-text-primary);
  box-sizing: border-box;
}

/* 备注卡片 */
.备注卡片 {
  background: var(--color-bg-card);
  /* Apple style: removed hard border, using shadow instead */
  border-radius: var(--radius-sm);
  padding: var(--card-padding);
  margin-bottom: 8px;
  box-shadow: var(--shadow-xs);
}
{
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}
{
  display: flex;
  align-items: center;
  gap: 8px;
  min-width: 0;
}
{
  width: 28px;
  height: 28px;
  border-radius: 50%;
  background: var(--color-primary);
  color: var(--color-text-white);
  font-size: var(--font-size-sm);
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
{
  display: flex;
  align-items: center;
  gap: 6px;
  min-width: 0;
}
{
  font-size: var(--font-size-sm);
  font-weight: 500;
  color: var(--color-text-primary);
}
{
  font-size: var(--font-size-xs);
  color: var(--color-text-tertiary);
  padding: 0 6px;
  height: 18px;
  line-height: 18px;
  border-radius: 9999px;
  background: var(--color-bg-page);
}
{
  font-size: var(--font-size-xs);
  color: var(--color-text-tertiary);
  white-space: nowrap;
  flex-shrink: 0;
}
{
  margin-top: 8px;
  font-size: var(--font-size-sm);
  color: var(--color-text-primary);
  line-height: 1.6;
  word-break: break-all;
}
{
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 8px;
}
{
  width: 72px;
  height: 72px;
  border-radius: var(--radius-xs);
  /* Apple: removed hard border */
}

/* ══ ⑬ 底部固定操作栏 ══ */
.-⑬-底部固定操作栏- {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 30;
  border-top: 1px solid var(--color-border-light);
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}
{
  display: flex;
  align-items: center;
  gap: 12px;
  padding: var(--spacing-xs) 16px;
  padding-bottom: calc(4px + env(safe-area-inset-bottom));
}
{
  flex: 1;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-sm);
  font-size: var(--font-size-lg);
  font-weight: 500;
  white-space: nowrap;
}
{
  /* Apple: removed hard border */
  background: var(--color-bg-card);
  color: var(--color-text-primary);
}
{
  background: transparent;
  color: var(--color-primary);
  border: 1.5px solid var(--color-primary);
}
{
  font-size: var(--font-size-lg);
}
