/**
 * 服装供应链系统 - 设计 Token  v3.0
 *
 * 视觉升级：更鲜明的蓝-靛渐变、更好的对比度、带蓝调的阴影
 *
 * @version 3.0
 * @date 2026-02-25
 */

/* 页面级 CSS 变量定义（组件通过 styleIsolation: apply-shared 继承）*/
page {
  /* ========== 颜色系统 ========== */

  /* 主色调 - 宝蓝（比之前更饱和，禁止使用紫色渐变）*/
  --color-primary: #2563eb;

  /* 浅色渐变 —— 用于卡片背景、tag填充（蓝-靛，更可见）*/
  --gradient-primary: linear-gradient(135deg, #dbeafe 0%, #ede9fe 100%);
  --gradient-primary-solid: linear-gradient(135deg, #dbeafe 0%, #ede9fe 100%);
  --gradient-primary-alpha: linear-gradient(
    135deg,
    rgba(219, 234, 254, 0.92) 0%,
    rgba(237, 233, 254, 0.75) 100%
  );
  --gradient-card: linear-gradient(135deg, #dbeafe 0%, #ede9fe 100%);

  /* 实色深蓝渐变 —— 专用于主 CTA 按钮（白字）*/
  --gradient-action: linear-gradient(135deg, #3b82f6 0%, #6366f1 100%);

  /* 进度条渐变（绿蓝渐变）*/
  --gradient-progress: linear-gradient(90deg, #10b981 0%, #2563eb 100%);

  /* 辅助色 - 蓝色系 */
  --color-blue: #2563eb;
  --color-blue-light: rgba(219, 234, 254, 0.8);
  --color-blue-lighter: rgba(219, 234, 254, 0.3);

  /* 功能色 */
  --color-success: #059669;
  --color-warning: #d97706;
  --color-error: #dc2626;
  --color-info: #2563eb;

  /* 中性色 —— 页面背景略带蓝调，与白色卡片形成清晰层次 */
  --color-bg-page: #eef2f8;
  --color-bg-card: #ffffff;
  --color-bg-gray: #e8ecf4;
  --color-bg-light: #f5f8ff;

  /* 边框 —— 稍微更可见的蓝色边框 */
  --color-border: rgba(96, 165, 250, 0.45);
  --color-border-light: rgba(147, 197, 253, 0.4);
  --color-border-blue: rgba(96, 165, 250, 0.45);

  /* 文字 —— 二级文字加深以改善对比度 */
  --color-text-primary: #0f172a;
  --color-text-secondary: #475569;
  --color-text-disabled: #94a3b8;
  --color-text-white: #ffffff;

  /* ========== 圆角系统 ========== */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 18px;
  --radius-full: 999px;

  /* ========== 间距系统 ========== */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 12px;
  --spacing-lg: 16px;
  --spacing-xl: 20px;
  --spacing-xxl: 24px;

  /* ========== 阴影系统 —— 带蓝调，卡片层次感更明显 ========== */
  --shadow-sm: 0 1px 3px rgba(15, 80, 180, 0.07);
  --shadow-md: 0 2px 10px rgba(15, 80, 180, 0.09);
  --shadow-lg: 0 4px 18px rgba(15, 80, 180, 0.11);
  --shadow-xl: 0 8px 28px rgba(15, 80, 180, 0.14);
  --shadow-modal: 0 20px 50px rgba(15, 80, 180, 0.18);

  /* ========== 字体系统 ========== */
  --font-size-xs: 12px;    /* 3级：辅助信息、标签 */
  --font-size-sm: 13px;    /* 2级：次要文字、说明 */
  --font-size-base: 15px;  /* 1级：主体内容 */
  --font-size-lg: 17px;    /* 大标题 */
  --font-size-xl: 19px;    /* 页面标题 */
  --font-size-2xl: 22px;   /* 超大展示数字 */

  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;
}
