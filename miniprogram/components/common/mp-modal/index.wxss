/* ============================================================
   mp-modal — 通用弹窗组件样式（v2: 参照扫码弹窗紧凑设计）
   负责：遮罩层 / 容器外形 / 标题栏
   body + actions 由各页面自定义，放入 slot 后自动继承页面 CSS
   设计规范：
     遮罩     rgba(0,0,0,0.45) / z-index 9999
     box宽度   90% / max-height 80vh（large=88vh）
     圆角     12rpx
     标题     28rpx / 600 / 居中 / 右侧绝对定位 ×
     动画     scale(0.92) → scale(1)  0.2s ease-out
   ============================================================ */

/* 遮罩层 —— fixed 全屏，flex 垂直水平居中 */
.mp-modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
}

/* 弹窗主体 */
.mp-modal-box {
  width: 90%;
  max-height: 80vh;
  background: #fff;
  border-radius: 12rpx;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  animation: mpModalIn 0.2s ease-out;
}

@keyframes mpModalIn {
  from { opacity: 0; transform: scale(0.92); }
  to   { opacity: 1; transform: scale(1); }
}

/* large 变体 */
.mp-modal-box.large {
  max-height: 88vh;
}

/* 标题栏 — 紧凑风格，参照扫码弹窗 */
.mp-modal-head {
  position: relative;
  padding: 16rpx 80rpx;
  text-align: center;
  font-size: 28rpx;
  font-weight: 600;
  color: #333;
  border-bottom: 1rpx solid #f0f0f0;
  flex-shrink: 0;
  line-height: 1.5;
}

.mp-modal-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #333;
}

/* 内容区 —— flex:1 占满剩余空间，scroll-view 原生滚动 */
.mp-modal-content {
  flex: 1;
  min-height: 0;
  padding: 0 20rpx;
}

/* 关闭按钮（右上角绝对定位） */
.mp-modal-close {
  position: absolute;
  right: 16rpx;
  top: 50%;
  transform: translateY(-50%);
  width: 48rpx;
  height: 48rpx;
  line-height: 48rpx;
  text-align: center;
  color: #aaa;
  font-size: 30rpx;
}
