<view class="global-search">
  <view class="search-box">
    <text class="search-icon">🔍</text>
    <input 
      class="search-input" 
      placeholder="{{placeholder}}" 
      value="{{keyword}}" 
      bindinput="onInput" 
    />
  </view>
  <view class="search-btn" bindtap="onSearch">搜索</view>
  <view class="clear-btn" bindtap="onClear">清空</view>
</view>

<view wx:if="{{hasSearched && results.length > 0}}" class="global-results">
  <view class="results-header">
    <text class="results-title">搜索结果 ({{results.length}})</text>
    <text class="close-results" bindtap="onClose">✕</text>
  </view>
  <scroll-view class="results-list" scroll-y>
    <view wx:for="{{results}}" wx:key="id" class="result-item" bindtap="onItemTap" data-item="{{item}}">
      <view class="result-header">
        <text class="result-type">{{item.typeText}}</text>
        <text class="result-status" style="color: {{item.statusColor}}">{{item.statusText}}</text>
      </view>
      <view class="result-info">
        <text class="result-order">订单号: {{item.orderNo}}</text>
        <text class="result-style">款号: {{item.styleNo}}</text>
      </view>
      <view wx:if="{{item.factoryName}}" class="result-detail">工厂: {{item.factoryName}}</view>
      <view wx:if="{{item.warehouse}}" class="result-detail">仓库: {{item.warehouse}}</view>
      <view wx:if="{{item.qualityStatusText}}" class="result-detail">质检: {{item.qualityStatusText}}</view>
    </view>
  </scroll-view>
</view>

<view wx:if="{{hasSearched && results.length === 0}}" class="no-results">
  <text class="no-results-icon">🔍</text>
  <text class="no-results-text">未找到匹配的订单</text>
  <view class="close-btn" bindtap="onClose">关闭</view>
</view>
