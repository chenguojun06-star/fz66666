@import '/styles/page-utils.wxss';

.page-container,
.container {
  min-height: 100vh;
  background: var(--color-bg-page);
  padding: 0 var(--page-padding-x) calc(20px + env(safe-area-inset-bottom));
}

/* ---- 跨岗位提示条 ---- */
.role-hint-bar {
  display: flex;
  align-items: center;
  gap: 5px;
  margin: var(--spacing-lg) 0 0;
  padding: var(--spacing-md) var(--spacing-lg);
  background: var(--color-warning-bg);
  border-radius: var(--radius-lg);
  border: 1px solid var(--color-warning-border);
}
.role-hint-icon { font-size: var(--font-size-sm); flex-shrink: 0; }
.role-hint-text { font-size: var(--font-size-xs); color: var(--color-warning); line-height: 1.5; }

/* ---- 筛选 Tab ---- */
.filter-tabs {
  display: flex;
  padding: var(--spacing-lg) 0;
  gap: var(--spacing-lg);
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}
.filter-tab {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  gap: 3px;
  padding: 8px 16px;
  border-radius: 20px;
  font-size: var(--font-size-sm);
  color: var(--color-text-secondary);
  background: var(--color-bg-card);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04), 0 1px 2px rgba(0, 0, 0, 0.03);
}
.filter-tab.active {
  background: var(--color-primary);
  color: var(--color-text-white);
  border-color: var(--color-primary);
}
.tab-badge {
  font-size: var(--font-size-xs);
  background: rgba(var(--color-primary-rgb), 0.3);
  padding: 1px 5px;
  border-radius: var(--radius-xl);
}
.filter-tab.active .tab-badge {
  background: rgba(255, 255, 255, 0.35);
}

/* ---- 加载状态 ---- */
.loading-state {
  text-align: center;
  padding: 60px 0;
  color: var(--color-text-secondary);
  font-size: var(--font-size-md);
}

/* ---- 订单卡片列表（对齐裁剪明细页面 order-sel-card 紧凑样式） ---- */
.order-list {
  padding: 0;
}

.order-sel-card {
  display: flex;
  align-items: flex-start;
  gap: var(--spacing-lg);
  padding: 14px 16px;
  background: var(--color-bg-card);
  border-radius: 16px;
  margin-bottom: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04), 0 1px 2px rgba(0, 0, 0, 0.03);
  position: relative;
}
.task-card-hover {
  transform: scale(0.99);
  box-shadow: var(--shadow-md);
}

.sel-cover-box {
  width: 60px;
  height: 80px;
  border-radius: var(--radius-md);
  overflow: hidden;
  flex-shrink: 0;
  background: var(--color-bg-gray);
}
.sel-cover-img {
  width: 100%;
  height: 100%;
}
.sel-cover-empty {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 13px;
  color: var(--color-text-tertiary);
}
.cover-abbr {
  font-size: 14px;
  color: var(--color-text-tertiary);
}

.sel-card-info {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

/* 顶部：订单号 + 状态标签 */
.info-top {
  display: flex;
  align-items: center;
  gap: 6px;
  flex-wrap: wrap;
  margin-bottom: 4px;
}
.order-no {
  font-size: 15px;
  font-weight: 600;
  color: var(--color-text-primary);
}
.sample-order-no {
  font-size: 13px;
  font-weight: 500;
}

/* 状态标签 */
.status-tag.tag {
  padding: 2px 8px;
  border-radius: var(--radius-sm);
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-medium);
  background: var(--color-primary-bg);
  color: var(--color-primary);
}
.status-tag.tag-warning { background: var(--color-warning-bg); color: var(--color-warning); }
.status-tag.tag-success { background: var(--color-success-bg); color: var(--color-success); }
.status-tag.tag-danger  { background: var(--color-danger-bg); color: var(--color-danger); }
.status-tag.tag-processing { background: var(--color-primary-bg); color: var(--color-primary); }

/* info-row：双行信息（label + value） */
.info-row {
  display: flex;
  align-items: center;
  gap: 6px;
  flex-wrap: wrap;
  margin-top: 2px;
}
.info-label {
  font-size: 11px;
  color: var(--color-text-tertiary);
  min-width: 32px;
}
.info-value {
  font-size: 13px;
  color: var(--color-text-primary);
  font-weight: 500;
}
.info-value.highlight {
  color: var(--color-primary);
  font-weight: 600;
}
.info-value.success {
  color: var(--color-success);
  font-weight: 600;
}
.info-value.pending {
  color: var(--color-warning);
  font-weight: 600;
}

/* 右侧箭头 */
.card-arrow {
  position: absolute;
  right: var(--spacing-xxl);
  top: 50%;
  transform: translateY(-50%);
  color: var(--color-text-tertiary);
  font-size: var(--font-size-lg);
  flex-shrink: 0;
}

/* ---- 底部提示 ---- */
.no-more {
  text-align: center;
  padding: 15px 0;
  color: var(--color-text-tertiary);
  font-size: var(--font-size-xs);
}
