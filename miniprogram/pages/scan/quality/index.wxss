.container {
  min-height: 100vh;
  background: var(--color-bg-page);
  padding: var(--page-padding-x) var(--page-padding-x) calc(env(safe-area-inset-bottom) + 80px);
}

/* ---- info-card ---- */
.info-card {
  background: var(--color-bg-card);
  border-radius: var(--radius-xl);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-lg);
  box-shadow: var(--shadow-sm);
}
.card-header { display: flex; align-items: flex-start; gap: var(--spacing-md); }
.cover-wrap {
  width: 70px; height: 70px;
  border-radius: var(--radius-lg); overflow: hidden;
  flex-shrink: 0; background: var(--color-border-light);
}
.header-info { display: flex; flex-direction: column; gap: var(--spacing-xs); flex: 1; min-width: 0; }
.style-no { font-size: var(--font-size-lg); font-weight: var(--font-weight-semibold); color: var(--color-text-primary); }
.sub-info { font-size: var(--font-size-xs); color: var(--color-text-tertiary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.tag-process {
  display: inline-block; align-self: flex-start;
  background: var(--color-primary-bg); color: var(--color-primary);
  font-size: var(--font-size-xs); padding: 2px 7px; border-radius: var(--radius-xs); margin-top: 2px;
}

/* ---- insight ---- */
.insight-card {
  display: flex; align-items: flex-start; gap: var(--spacing-xs);
  border-radius: var(--radius-lg); padding: var(--spacing-md) var(--spacing-lg); margin-bottom: var(--spacing-lg);
  background: var(--color-primary-bg); color: var(--color-primary);
  font-size: var(--font-size-xs); line-height: var(--line-height-relaxed);
}
.insight-icon { flex-shrink: 0; font-size: var(--font-size-base); }
.insight-body { display: flex; flex-direction: column; gap: var(--spacing-xs); flex: 1; }
.insight-text { font-size: var(--font-size-xs); }
.insight-urgent { color: var(--color-danger); font-weight: var(--font-weight-medium); }
.insight-label { font-size: var(--font-size-xs); font-weight: var(--font-weight-semibold); color: var(--color-text-primary); margin-top: 2px; }
.insight-checkpoint { font-size: var(--font-size-xs); color: var(--color-text-secondary); line-height: var(--line-height-relaxed); }
.insight-action {
  align-self: flex-start; margin-top: var(--spacing-xs);
  font-size: var(--font-size-xs); font-weight: var(--font-weight-semibold);
  color: var(--color-primary);
  padding: var(--spacing-xs) var(--spacing-md); border-radius: var(--radius-xs);
  background: var(--color-primary-bg);
}

/* ---- section ---- */
.section-card {
  background: var(--color-bg-card); border-radius: var(--radius-xl);
  padding: var(--spacing-md) var(--spacing-lg); margin-bottom: var(--spacing-lg);
  box-shadow: var(--shadow-sm);
}
.section-title { font-size: var(--font-size-sm); font-weight: var(--font-weight-semibold); color: var(--color-text-primary); margin-bottom: var(--spacing-md); display: block; }

/* ---- quality result ---- */
.result-group { display: flex; gap: var(--spacing-lg); }
.result-btn {
  flex: 1; display: flex; align-items: center; justify-content: center; gap: var(--spacing-xs);
  height: 50px; border-radius: var(--radius-lg);
  background: var(--color-bg-light); font-size: var(--font-size-base); color: var(--color-text-secondary);
  border: 1px solid transparent; transition: all 0.2s;
}
.result-btn.active-pass {
  background: var(--color-success-bg); color: var(--color-success);
  border-color: var(--color-success); font-weight: var(--font-weight-semibold);
}
.result-btn.active-fail {
  background: var(--color-danger-bg); color: var(--color-danger);
  border-color: var(--color-danger); font-weight: var(--font-weight-semibold);
}
.result-icon { font-size: var(--font-size-xl); }

/* ---- form ---- */
.form-row {
  display: flex; align-items: center; justify-content: space-between;
  padding: var(--spacing-md) 0; border-bottom: 1px solid var(--color-border-light);
}
.form-row:last-child { border-bottom: none; }
.form-row-col {
  display: flex; flex-direction: column; gap: var(--spacing-xs);
  padding: var(--spacing-md) 0;
}
.form-label { font-size: var(--font-size-sm); color: var(--color-text-primary); flex-shrink: 0; }
.form-input {
  width: 130px; height: 36px; text-align: right;
  border: 1px solid var(--color-border); border-radius: var(--radius-sm);
  padding: 0 var(--spacing-sm); font-size: var(--font-size-md);
  background: var(--color-bg-card);
}
.form-picker {
  display: flex; align-items: center; gap: var(--spacing-xs);
  font-size: var(--font-size-sm); color: var(--color-text-primary);
}
.picker-arrow { font-size: var(--font-size-xs); color: var(--color-text-tertiary); }

/* ---- photo grid ---- */
.photo-grid { display: flex; flex-wrap: wrap; gap: var(--spacing-md); }
.photo-item { position: relative; width: 80px; height: 80px; border-radius: var(--radius-md); overflow: hidden; }
.photo-img { width: 100%; height: 100%; }
.photo-del {
  position: absolute; top: 2px; right: 2px;
  width: 20px; height: 20px; border-radius: var(--radius-full);
  background: var(--color-overlay); color: var(--color-text-white);
  display: flex; align-items: center; justify-content: center;
  font-size: var(--font-size-md); line-height: 1;
}
.photo-add {
  width: 80px; height: 80px; border-radius: var(--radius-md);
  border: 1px solid var(--color-border); display: flex; flex-direction: column;
  align-items: center; justify-content: center; gap: var(--spacing-xs);
  background: var(--color-bg-gray);
}
.add-icon { font-size: var(--font-size-xl); }
.add-text { font-size: var(--font-size-xs); color: var(--color-text-tertiary); }

/* ---- remark ---- */
.remark-area {
  width: 100%; min-height: 60px;
  padding: var(--spacing-xs); font-size: var(--font-size-sm);
  border: 1px solid var(--color-border); border-radius: var(--radius-md);
  box-sizing: border-box;
}

/* ---- AI image analysis ---- */
.ai-image-result {
  margin-top: var(--spacing-lg);
  background: var(--color-primary-bg);
  border-radius: var(--radius-lg);
  padding: var(--spacing-md);
}
.ai-image-header {
  display: flex; align-items: center; gap: var(--spacing-xs);
  margin-bottom: var(--spacing-sm);
}
.ai-image-icon { font-size: var(--font-size-base); }
.ai-image-title {
  font-size: var(--font-size-sm); font-weight: var(--font-weight-semibold);
  color: var(--color-text-primary); flex: 1;
}
.ai-image-adopt, .ai-image-retry {
  font-size: var(--font-size-xs); color: var(--color-primary);
  padding: var(--spacing-xs) var(--spacing-sm); border-radius: var(--radius-xs);
  background: var(--color-primary-bg);
  font-weight: var(--font-weight-medium);
}
.ai-image-loading {
  font-size: var(--font-size-xs); color: var(--color-text-secondary);
  padding: var(--spacing-xs) 0; text-align: center;
}
.ai-image-error {
  font-size: var(--font-size-xs); color: var(--color-danger);
  padding: 4px 0;
}
.ai-image-defects { display: flex; flex-direction: column; gap: var(--spacing-xs); }
.ai-severity {
  display: flex; align-items: center; gap: var(--spacing-xs);
  font-size: var(--font-size-xs); color: var(--color-text-secondary);
}
.severity-badge {
  font-size: var(--font-size-xs); font-weight: var(--font-weight-semibold);
  padding: 2px 6px; border-radius: var(--radius-xs);
}
.severity-CRITICAL { background: var(--color-danger-bg); color: var(--color-danger); }
.severity-HIGH { background: var(--color-warning-bg); color: var(--color-warning); }
.severity-MEDIUM { background: var(--color-warning-bg); color: var(--color-warning); }
.severity-LOW { background: var(--color-success-bg); color: var(--color-success); }
.severity-NONE { background: var(--color-success-bg); color: var(--color-success); }
.defect-item {
  display: flex; flex-direction: column; gap: 2px;
  padding-left: var(--spacing-xs);
}
.defect-type { font-size: var(--font-size-xs); color: var(--color-text-primary); font-weight: var(--font-weight-medium); }
.defect-desc { font-size: var(--font-size-xs); color: var(--color-text-secondary); }
.defect-confidence { font-size: var(--font-size-xs); color: var(--color-text-tertiary); }
.ai-image-report, .ai-image-recommend {
  margin-top: var(--spacing-xs); padding-top: var(--spacing-xs);
  border-top: 1px solid var(--color-border-light);
}
.report-label, .recommend-label {
  font-size: var(--font-size-xs); color: var(--color-text-secondary);
  display: block; margin-bottom: 2px;
}
.report-text, .recommend-text {
  font-size: var(--font-size-xs); color: var(--color-text-primary);
  line-height: var(--line-height-relaxed);
}
.ai-image-clean {
  font-size: var(--font-size-xs); color: var(--color-success);
  padding: var(--spacing-xs) 0; text-align: center;
}
