/* 🆕 扫码结果确认弹窗样式（2026-02-06 混合模式） */
.scan-result-modal .modal-box {
  width: 85%;
  max-width: 400px;
}

.scan-result-info {
  padding: 12px 0;
}

.result-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 16px;
  margin-bottom: 8px;
  background: #f8f9fa;
  border-radius: 8px;
}

.result-item.process-selector {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
}

.result-item.process-selector .item-label {
  color: #fff;
  font-weight: 600;
}

.item-label {
  font-size: 13px;
  color: #64748b;
  font-weight: 500;
}

.item-value {
  font-size: var(--font-size-base);
  color: #1e293b;
  font-weight: 600;
}

.item-value.highlight {
  color: #2563eb;
}

.process-picker {
  flex: 1;
  text-align: right;
}

.picker-display {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 6px 12px;
  background: rgba(255, 255, 255, 0.95);
  border-radius: 6px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.picker-value {
  font-size: 15px;
  color: #1e293b;
  font-weight: 700;
}

.picker-arrow {
  font-size: 10px;
  color: #64748b;
}

.result-hint {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px;
  margin: 12px 0 0 0;
  background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
  border-radius: 8px;
  border-left: 4px solid #f59e0b;
}

.hint-icon {
  font-size: var(--font-size-xl);
}

.hint-text {
  flex: 1;
  font-size: var(--font-size-sm);
  color: #92400e;
  line-height: 1.5;
}

.modal-btn.ok {
  background: linear-gradient(135deg, #10b981 0%, var(--color-success) 100%);
  box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
}

.modal-btn.ok.loading {
  background: #9ca3af;
  pointer-events: none;
}
