# 卡片视图快速集成指南

## 已完成的页面
- ✅ 样板生产（PatternProduction）
- ✅ 样衣管理（StyleInfo）  
- ✅ 我的订单（ProductionList）

## 待完成的页面集成代码

以下代码已准备好，只需要复制到对应位置即可。

---

### 1. 订单管理（OrderManagement）

**添加状态**（在 line 89 后添加）：
```tsx
const [viewMode, setViewMode] = useState<'list' | 'card'>('list');
```

**添加切换按钮**（在页面标题旁边，找到 "page-header" 部分）：
```tsx
<Button
  icon={viewMode === 'list' ? <AppstoreOutlined /> : <UnorderedListOutlined />}
  onClick={() => setViewMode(viewMode === 'list' ? 'card' : 'list')}
>
  {viewMode === 'list' ? '卡片视图' : '列表视图'}
</Button>
```

**添加卡片视图**（替换 ResizableTable 部分）：
```tsx
{viewMode === 'list' ? (
  <ResizableTable ... />
) : (
  <UniversalCardView
    dataSource={styles}
    loading={loading}
    columns={4}
    coverField="cover"
    titleField="styleNo"
    subtitleField="styleName"
    fields={[
      { label: '客户', key: 'customer' },
      { label: '季节', key: 'season' },
      { label: '类别', key: 'category', render: (val) => toCategoryCn(val) },
      { label: '交期', key: 'deliveryDate', render: (val) => val ? formatDateTime(val) : '-' },
    ]}
    progressConfig={{ show: false }}
    actions={(record) => [
      {
        key: 'order',
        icon: <ShoppingCartOutlined />,
        label: '下单',
        onClick: () => openOrderDialog(record),
      },
    ]}
  />
)}
```

---

### 2. 数据中心（DataCenter）

**添加状态**（在 useState 部分添加）：
```tsx
const [viewMode, setViewMode] = useState<'list' | 'card'>('list');
```

**添加切换按钮**（在页面顶部操作区）：
```tsx
<Button
  icon={viewMode === 'list' ? <AppstoreOutlined /> : <UnorderedListOutlined />}
  onClick={() => setViewMode(viewMode === 'list' ? 'card' : 'list')}
>
  {viewMode === 'list' ? '卡片视图' : '列表视图'}
</Button>
```

**添加卡片视图**：
```tsx
{viewMode === 'list' ? (
  <ResizableTable ... />
) : (
  <UniversalCardView
    dataSource={data}
    loading={loading}
    columns={6}
    coverField="cover"
    titleField="styleNo"
    subtitleField="styleName"
    fields={[
      { label: '类别', key: 'category', render: (val) => toCategoryCn(val) },
      { label: '季节', key: 'season' },
      { label: '码数', key: 'size' },
      { label: '颜色', key: 'color' },
    ]}
    progressConfig={{ show: false }}
    actions={(record) => [
      {
        key: 'print',
        icon: <PrinterOutlined />,
        label: '打印生产单',
        onClick: () => handlePrintProductionSheet(record.id),
      },
      {
        key: 'download',
        icon: <DownloadOutlined />,
        label: '附件管理',
        onClick: () => {}, // 或使用 StyleAttachmentsButton
      },
    ]}
  />
)}
```

---

### 3. 物料采购（MaterialPurchase）

**注意**：此页面有多个 Tab，建议只在"面辅料采购"tab添加卡片视图

**添加状态**（在现有状态后添加）：
```tsx
const [viewMode, setViewMode] = useState<'list' | 'card'>('list');
```

**添加切换按钮**（在每个tab的操作区）：
```tsx
<Button
  icon={viewMode === 'list' ? <AppstoreOutlined /> : <UnorderedListOutlined />}
  onClick={() => setViewMode(viewMode === 'list' ? 'card' : 'list')}
>
  {viewMode === 'list' ? '卡片视图' : '列表视图'}
</Button>
```

**添加卡片视图**：
```tsx
{viewMode === 'list' ? (
  <ResizableTable ... />
) : (
  <UniversalCardView
    dataSource={materialList}
    loading={loading}
    columns={4}
    titleField="orderNo"
    subtitleField="styleName"
    fields={[
      { label: '工厂', key: 'factoryName' },
      { label: '物料类型', key: 'materialType' },
      { label: '物料名称', key: 'materialName' },
      { label: '数量', key: 'quantity', suffix: ' 单位' },
    ]}
    progressConfig={{ show: false }}
    actions={(record) => [
      {
        key: 'view',
        icon: <EyeOutlined />,
        label: '查看详情',
        onClick: () => handleView(record),
      },
      {
        key: 'edit',
        icon: <EditOutlined />,
        label: '编辑',
        onClick: () => handleEdit(record),
      },
    ]}
  />
)}
```

---

### 4. 裁剪管理（Cutting）

**添加状态**：
```tsx
const [viewMode, setViewMode] = useState<'list' | 'card'>('list');
```

**添加切换按钮**：
```tsx
<Button
  icon={viewMode === 'list' ? <AppstoreOutlined /> : <UnorderedListOutlined />}
  onClick={() => setViewMode(viewMode === 'list' ? 'card' : 'list')}
>
  {viewMode === 'list' ? '卡片视图' : '列表视图'}
</Button>
```

**添加卡片视图**：
```tsx
{viewMode === 'list' ? (
  <ResizableTable ... />
) : (
  <UniversalCardView
    dataSource={cuttingTasks}
    loading={loading}
    columns={4}
    titleField="orderNo"
    subtitleField="styleNo"
    fields={[
      { label: '工厂', key: 'factoryName' },
      { label: '颜色', key: 'color' },
      { label: '菲号', key: 'bundleCount', suffix: ' 个' },
      { label: '数量', key: 'totalQuantity', suffix: ' 件' },
    ]}
    progressConfig={{ show: false }}
    actions={(record) => [
      {
        key: 'view',
        icon: <EyeOutlined />,
        label: '查看详情',
        onClick: () => handleView(record),
      },
      {
        key: 'print',
        icon: <PrinterOutlined />,
        label: '打印二维码',
        onClick: () => handlePrintQRCode(record),
      },
    ]}
  />
)}
```

---

### 5. 生产进度（ProgressDetail）

**添加状态**：
```tsx
const [viewMode, setViewMode] = useState<'list' | 'card'>('list');
```

**添加切换按钮**：
```tsx
<Button
  icon={viewMode === 'list' ? <AppstoreOutlined /> : <UnorderedListOutlined />}
  onClick={() => setViewMode(viewMode === 'list' ? 'card' : 'list')}
>
  {viewMode === 'list' ? '卡片视图' : '列表视图'}
</Button>
```

**添加卡片视图**：
```tsx
{viewMode === 'list' ? (
  <ResizableTable ... />
) : (
  <UniversalCardView
    dataSource={progressList}
    loading={loading}
    columns={4}
    titleField="orderNo"
    subtitleField="styleNo"
    fields={[
      { label: '工厂', key: 'factoryName' },
      { label: '当前工序', key: 'currentProcess' },
      { label: '完成数量', key: 'completedQuantity', suffix: ' 件' },
      { label: '总数量', key: 'totalQuantity', suffix: ' 件' },
    ]}
    progressConfig={{
      calculate: (record) => Math.round((record.completedQuantity / record.totalQuantity) * 100),
      show: true,
    }}
    actions={(record) => [
      {
        key: 'view',
        icon: <EyeOutlined />,
        label: '查看详情',
        onClick: () => handleView(record),
      },
      {
        key: 'scan',
        icon: <ScanOutlined />,
        label: '扫码记录',
        onClick: () => handleViewScanRecords(record),
      },
    ]}
  />
)}
```

---

### 6. 质检入库（ProductWarehousing）

**注意**：此页面业务逻辑复杂，建议暂不添加卡片视图，或仅在列表页添加

---

## 集成检查清单

对于每个页面，确保：
- [ ] ✅ 已导入 `UniversalCardView` 和图标
- [ ] ✅ 已添加 `viewMode` 状态
- [ ] ✅ 已添加切换按钮
- [ ] ✅ 已添加条件渲染逻辑
- [ ] ✅ 配置了合适的字段
- [ ] ✅ 配置了操作按钮
- [ ] ✅ 测试了卡片点击事件（如需要）

---

## 通用配置建议

| 页面类型 | 建议列数 | 进度条 | 封面图 |
|---------|---------|--------|--------|
| 订单类 | 4 | 可选 | 建议有 |
| 款式类 | 6 | 否 | 必须有 |
| 物料类 | 4 | 否 | 可选 |
| 进度类 | 4 | 是 | 建议有 |

---

**维护者**：GitHub Copilot  
**最后更新**：2026年1月28日
