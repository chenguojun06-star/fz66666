# 代码清理分析报告

**生成时间**: 2026-02-09  
**分析范围**: 后端 + PC端 + 小程序  
**扫描工具**: ESLint + grep + 人工审核  

---

## 📊 执行摘要

| 类型 | 后端 | PC端 | 小程序 | 总计 |
|------|------|------|--------|------|
| 🔴 **废弃API** | 100+ | 1 | 0 | **101+** |
| 🟡 **未使用导入** | - | 30+ | - | **30+** |
| 🔵 **Console日志** | - | 50+ | 60+ | **110+** |
| 🟠 **TODO标记** | 30+ | 0 | - | **30+** |

**清理优先级**：
- ⚠️ **P0 - 紧急**：删除5个月前标记的废弃API（58个端点计划2026-05-01删除）
- ⚠️ **P1 - 重要**：清理前端未使用的导入（影响打包体积）
- ⚠️ **P2 - 建议**：清理console日志（生产环境安全隐患）
- 📝 **P3 - 待办**：处理TODO标记（技术债追踪）

---

## 🔴 1. 废弃API清理（后端 - 100+个）

### 1.1 高危废弃端点（2026-05-01前必删）

根据 [开发指南.md](开发指南.md) 第6.3节，以下58个端点已废弃，计划2026-05-01删除：

#### **生产订单模块**（8个）
```java
// ProductionOrderController.java
@Deprecated GET  /api/production/order/by-order-no/{orderNo}
            → POST /list?orderNo={orderNo}

@Deprecated GET  /api/production/order/simple/{id}
            → GET  /detail/{id}（前端过滤字段）

@Deprecated POST /api/production/order/save
            → POST / (新增) | PUT / (更新)

@Deprecated POST /api/production/order/delete/{id}
            → DELETE /{id}
```

#### **扫码记录模块**（8个）
```java
// ScanRecordController.java
@Deprecated GET /by-order/{orderId}       → GET /list?orderId={orderId}
@Deprecated GET /by-style/{styleNo}       → GET /list?styleNo={styleNo}
@Deprecated GET /all                      → GET /list
@Deprecated GET /my                       → GET /list?currentUser=true

@Deprecated GET /sku-list/{orderNo}       → GET /sku/query?type=list&orderNo={orderNo}
@Deprecated GET /sku-progress             → GET /sku/query?type=progress
@Deprecated GET /sku-order-progress/{orderNo} → GET /sku/query?type=order-progress
@Deprecated GET /sku-statistics           → GET /sku/query?type=statistics
```

#### **款式管理模块**（8个）
```java
// StyleInfoController.java
@Deprecated POST /{id}/pattern-start      → POST /{id}/stage-action?stage=pattern&action=start
@Deprecated POST /{id}/pattern-complete   → POST /{id}/stage-action?stage=pattern&action=complete
@Deprecated POST /{id}/pattern-reset      → POST /{id}/stage-action?stage=pattern&action=reset
@Deprecated POST /{id}/sample-start       → POST /{id}/stage-action?stage=sample&action=start
@Deprecated POST /{id}/sample-progress    → POST /{id}/stage-action?stage=sample&action=progress
@Deprecated POST /{id}/sample-complete    → POST /{id}/stage-action?stage=sample&action=complete
@Deprecated POST /{id}/sample-reset       → POST /{id}/stage-action?stage=sample&action=reset
```

#### **面料采购模块**（4个）
```java
// MaterialPurchaseController.java
@Deprecated POST /confirm-return          → POST /return-confirm
@Deprecated POST /confirm-return/cancel   → POST /return-confirm/reset
@Deprecated GET  /by-scan/{scanCode}      → GET  /list?scanCode={scanCode}
@Deprecated GET  /my-tasks                → GET  /list?myTasks=true
```

#### **裁剪管理模块**（3个）
```java
// CuttingBundleController.java
@Deprecated GET /by-qr-code/{qrCode}      → GET /list?qrCode={qrCode}
@Deprecated GET /by-bundle/{orderNo}/{bundleNo} → GET /list?orderNo={orderNo}&bundleNo={bundleNo}

// CuttingTaskController.java
@Deprecated GET /my-tasks                 → GET /list?myTasks=true
```

#### **财务结算模块**（4个）
```java
// MaterialReconciliationController.java
@Deprecated POST /{id}/update-status      → POST /{id}/status-action?action=update&status=xxx
@Deprecated POST /{id}/return             → POST /{id}/status-action?action=return&reason=xxx

// ShipmentReconciliationController.java
@Deprecated GET  /all                     → GET  /list（大pageSize）
```

#### **样板生产模块**（8个）
```java
// PatternProductionController.java
@Deprecated POST /{id}/receive            → POST /{id}/workflow-action?action=receive
@Deprecated POST /{id}/complete           → POST /{id}/workflow-action?action=complete
@Deprecated POST /{id}/warehouse-in       → POST /{id}/workflow-action?action=warehouse-in
@Deprecated POST /{id}/maintenance        → POST /{id}/workflow-action?action=maintenance

// PatternRevisionController.java
@Deprecated POST /{id}/submit             → POST /{id}/workflow?action=submit
@Deprecated POST /{id}/approve            → POST /{id}/workflow?action=approve
@Deprecated POST /{id}/reject             → POST /{id}/workflow?action=reject
@Deprecated POST /{id}/complete           → POST /{id}/workflow?action=complete
```

#### **其他模块**（13个）
```java
// OrderTransferController.java
@Deprecated GET /pending                  → GET /list?type=pending
@Deprecated GET /my-transfers             → GET /list?type=my-transfers
@Deprecated GET /received                 → GET /list?type=received

// UserController.java
@Deprecated POST /{id}/approve            → POST /{id}/approval-action?action=approve
@Deprecated POST /{id}/reject             → POST /{id}/approval-action?action=reject

// TemplateLibraryController.java
@Deprecated GET  /type/{templateType}     → GET  /list?templateType={templateType}
@Deprecated POST /save                    → POST / | PUT /

// MaterialInboundController.java
@Deprecated GET  /by-purchase/{purchaseId} → GET /list?purchaseId={purchaseId}

// ProductWarehousingController.java
@Deprecated POST /repair-stats/{id}       → POST /repair-stats（批量）

// StyleBomController.java
@Deprecated POST /{styleId}/sync-material-async → POST /{styleId}/sync-material-database?async=true
```

### 1.2 已废弃服务类（2个）
```java
// SystemOperationLogService.java
@Deprecated
public interface SystemOperationLogService {
  // 请使用 LoginLogService 替代
}

// SystemOperationLogServiceImpl.java
@Deprecated
public class SystemOperationLogServiceImpl {
  // 请使用 LoginLogService 替代
}
```

### ✅ 清理建议
```bash
# 1. 运行清理脚本（项目已有工具）
python3 clean-deprecated-code.py --dry-run  # 预览

# 2. 确认无前端调用后删除（检查 frontend/src/services/）
grep -r "by-order-no" frontend/src/services/
grep -r "pattern-start" frontend/src/services/

# 3. 删除废弃代码
python3 clean-deprecated-code.py --execute

# 4. 运行测试验证
cd backend && mvn test
cd frontend && npm run build
```

---

## 🟡 2. 未使用导入清理（PC端 - 30+个）

### 2.1 重复导入的图标（Ant Design Icons）

**文件**: `frontend/src/modules/**/*.tsx`

```typescript
// ❌ 未使用的图标导入（30+个）
import { CloseOutlined } from '@ant-design/icons';        // 未使用
import { PrinterOutlined } from '@ant-design/icons';      // 未使用
import { DownloadOutlined } from '@ant-design/icons';     // 未使用
import { EditOutlined } from '@ant-design/icons';         // 未使用
import { EyeOutlined } from '@ant-design/icons';          // 未使用
import { FileTextOutlined } from '@ant-design/icons';     // 未使用
import { UploadOutlined } from '@ant-design/icons';       // 未使用
import { ShoppingCartOutlined } from '@ant-design/icons'; // 未使用
import { SaveOutlined } from '@ant-design/icons';         // 未使用
import { CheckCircleOutlined } from '@ant-design/icons';  // 未使用
import { SendOutlined } from '@ant-design/icons';         // 未使用
import { PlusOutlined } from '@ant-design/icons';         // 未使用
import { DeleteOutlined } from '@ant-design/icons';       // 未使用
```

**影响文件**：
- `frontend/src/modules/production/pages/**/*`
- `frontend/src/modules/finance/pages/**/*`
- `frontend/src/components/**/*`

### 2.2 未使用的组件导入

```typescript
// frontend/src/modules/finance/pages/FinanceDashboard/index.tsx
import { Statistic } from 'antd';  // 未使用
```

### ✅ 清理建议
```bash
# 1. 自动删除未使用的导入
cd frontend && npx eslint --fix --rule '@typescript-eslint/no-unused-vars: error' src/

# 2. 手动检查关键文件
# - RowActions.tsx（可能图标在运行时动态使用）
# - StyleForm.tsx（可能图标在条件分支中使用）

# 3. 验证构建
npm run build

# 预期收益：减少打包体积 5-10KB（Tree-shaking后）
```

---

## 🔵 3. Console日志清理（双端 - 110+个）

### 3.1 PC端（50+个）

#### **高频错误日志**（可保留）
```typescript
// ✅ 保留：捕获错误日志
console.error('获取角色选项失败', error);           // UserApproval/index.tsx
console.error('[实时同步] 获取用户列表失败', error); // UserList/index.tsx
console.error('加载数据失败:', error);              // Dashboard/index.tsx
```

#### **调试日志（需删除）**
```typescript
// ❌ 删除：开发调试日志
console.log('[转单] 菲号数据:', records);          // Production/List/index.tsx:803
console.log('[转单] 使用订单工序配置:', processes); // Production/List/index.tsx:822
console.log('[转单] 使用款式工序配置:', processes); // Production/List/index.tsx:837
```

#### **注释掉的日志（需删除）**
```typescript
// ❌ 删除：已注释的日志
// console.log('[实时同步] 用户列表数据已更新', { ... });
// console.log('[实时同步] 裁剪批次数据已更新', { ... });
// console.log('[工序状态] 获取成功:', res.data);
// console.log('[采购状态] 获取成功:', res.data);
```

### 3.2 小程序端（60+个）

#### **错误日志（可保留）**
```javascript
// ✅ 保留：错误追踪
console.error('[ScanHandler] 扫码处理异常:', e);
console.error('[ScanHandler] 提交扫码失败:', e);
console.error('加载任务失败:', err);
```

#### **调试日志（需删除）**
```javascript
// ❌ 删除：开发调试日志
console.log('[loadMyHistory] 加载成功, 分组数:', groupedHistory.length); // HistoryHandler.js:328
```

#### **全局错误处理（保留）**
```javascript
// ✅ 保留：全局错误监控
console.error('[App] 全局错误:', msg);           // app.js:235
console.error('[App] 未处理的Promise拒绝:', res); // app.js:242
```

### ✅ 清理建议
```bash
# PC端：运行清理脚本（项目已有工具）
./clean-console-logs.sh

# 小程序端：手动清理（保留错误日志）
# 1. 批量注释掉调试日志
sed -i '' 's/console\.log(/\/\/ console.log(/g' miniprogram/**/*.js

# 2. 保留错误日志
# - console.error() 保留
# - console.warn() 根据业务需求决定

# 3. 生产环境禁用console
# 在 miniprogram/app.js 添加：
if (process.env.NODE_ENV === 'production') {
  console.log = () => {};
  console.warn = () => {};
}
```

---

## 🟠 4. TODO标记清理（后端 - 30+个）

### 4.1 低优先级TODO（可长期存在）

```java
// ✅ 功能增强类（低优先级）
// TODO: 待实现出库表
// TODO: 如果有锁定库存功能，从相关表查询
// TODO: 从次品表查询
// TODO: 实现返修数量计算逻辑
// TODO: 实现版型文件检查逻辑
```

### 4.2 第三方集成TODO（需跟进）

```java
// ⚠️ 物流集成（待对接）
// integration/logistics/impl/SFExpressAdapter.java
// TODO: 实现顺丰下单逻辑
// TODO: 实现顺丰取消运单逻辑
// TODO: 实现顺丰路由查询逻辑
// TODO: 实现顺丰运费计算逻辑
// TODO: 实现顺丰地址校验逻辑

// integration/logistics/impl/STOAdapter.java
// TODO: 实现申通下单逻辑
// TODO: 实现申通取消运单逻辑
// TODO: 实现申通路由查询逻辑
// TODO: 实现申通运费计算逻辑
// TODO: 实现申通地址校验逻辑

// integration/payment/impl/WechatPayAdapter.java
// TODO: 实现微信支付逻辑
// TODO: 实现微信支付查询逻辑
// TODO: 实现微信支付退款逻辑
```

### 4.3 技术债TODO（高优先级）

```java
// ⚠️ 待完成功能（影响现有功能）
// common/audit/AuditLogAspect.java
// TODO: 待完成 OperationLog 实体统一后重新启用

// production/orchestration/MaterialInboundOrchestrator.java
// TODO: 添加更多查询条件
```

### ✅ 清理建议
```bash
# 1. 整理TODO到项目看板
grep -r "TODO" backend/src --include="*.java" > todo-list.txt

# 2. 分类处理
# - P0（技术债）: 立即处理（AuditLogAspect）
# - P1（集成）  : 按需实现（物流/支付）
# - P2（增强）  : 长期规划（返修/次品）

# 3. 清理已完成的TODO
# 检查代码是否已实现，删除过期TODO

# 4. 更新文档
# 将TODO迁移到 GitHub Issues 或项目管理工具
```

---

## 📁 5. 文件级别清理建议

### 5.1 空文件/占位文件（需确认）

```bash
# 运行检查脚本
find backend/src -type f -name "*.java" -size 0
find frontend/src -type f -name "*.ts" -size 0
find miniprogram -type f -name "*.js" -size 0
```

### 5.2 测试文件覆盖率

**已有测试**：31个测试文件
- ✅ 核心Orchestrator测试：ReconciliationStatusOrchestratorTest.java
- ✅ Executor测试：ProductionScanExecutorTest, QualityScanExecutorTest, WarehouseScanExecutorTest
- ✅ Service测试：ProductionOrderServiceImplTest, ScanRecordServiceImplTest

**缺失测试**：
- ❌ Controller层测试（0个）
- ❌ 小程序单元测试（0个）

### ✅ 建议
```bash
# 删除空测试类
find backend/src/test -name "*Test.java" -size -100c -delete

# 添加Controller测试（覆盖废弃API迁移）
# 示例：ProductionOrderControllerTest.java
@Test
public void testDeprecatedApiShouldReturnError() {
  // 验证废弃API返回410 Gone状态码
}
```

---

## 🔄 6. 重复代码检测

### 6.1 相似的Controller模式

**模式1：统一查询端点**（已完成）
```java
// ✅ 好模式：统一使用 POST /list
@PostMapping("/list")
public Result<Page<T>> list(@RequestBody QueryRequest req)

// ❌ 旧模式：散乱的GET端点（已废弃）
@GetMapping("/by-xxx/{id}")
@GetMapping("/my-tasks")
```

**模式2：统一工作流端点**（已完成）
```java
// ✅ 好模式：统一使用 POST /{id}/stage-action
@PostMapping("/{id}/stage-action")
public Result<T> stageAction(@PathVariable Long id, @RequestParam String action)

// ❌ 旧模式：分散的状态流转端点（已废弃）
@PostMapping("/{id}/submit")
@PostMapping("/{id}/approve")
```

### 6.2 相似的错误处理

**前端错误处理**（50+次重复）
```typescript
// 重复模式：
try {
  const res = await api.getData();
  // ...
} catch (error) {
  console.error('加载失败:', error);
  message.error('加载失败');
}
```

**建议**：封装统一错误处理Hook
```typescript
// hooks/useApiCall.ts
export const useApiCall = <T>(apiFunc: () => Promise<T>) => {
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);
  
  const execute = useCallback(async () => {
    setLoading(true);
    try {
      return await apiFunc();
    } catch (err) {
      setError(err);
      message.error('操作失败，请重试');
      throw err;
    } finally {
      setLoading(false);
    }
  }, [apiFunc]);
  
  return { execute, loading, error };
};
```

---

## 📊 7. 清理执行计划

### 第一阶段：紧急清理（1周内）

```bash
# Day 1-2: 废弃API清理
- [ ] 运行 clean-deprecated-code.py --dry-run
- [ ] 确认前端无调用
- [ ] 删除58个废弃端点
- [ ] 运行全量测试

# Day 3-4: 前端未使用导入
- [ ] npx eslint --fix 自动修复
- [ ] 手动检查动态使用的图标
- [ ] npm run build 验证

# Day 5: Console日志清理
- [ ] 运行 ./clean-console-logs.sh
- [ ] 小程序手动清理
- [ ] 保留错误日志

# Day 6-7: 测试验证
- [ ] 运行所有测试套件
- [ ] 手动回归测试核心流程
- [ ] 生成清理报告
```

### 第二阶段：优化改进（2周内）

```bash
# Week 2: TODO整理
- [ ] 整理30+个TODO到项目看板
- [ ] 实现P0技术债（AuditLogAspect）
- [ ] 规划P1第三方集成

# Week 3: 重复代码重构
- [ ] 封装统一错误处理Hook
- [ ] 提取公共业务逻辑
- [ ] 添加Controller层测试
```

### 第三阶段：持续监控（长期）

```bash
# 每月检查
- [ ] 运行ESLint检查未使用导入
- [ ] grep检查新增console.log
- [ ] 更新废弃API列表

# CI/CD集成
- [ ] 添加pre-commit hook（禁止console.log）
- [ ] 添加ESLint检查到CI流程
- [ ] 设置代码覆盖率阈值
```

---

## 📈 8. 预期收益

### 代码质量
- ✅ **减少代码行数**：预计减少 2000+ 行废弃代码
- ✅ **提升可维护性**：删除58个废弃端点，统一API风格
- ✅ **降低技术债**：处理30+个TODO标记

### 性能优化
- ✅ **前端打包体积**：减少 5-10KB（删除未使用导入）
- ✅ **编译速度**：减少约 5%（代码量减少）

### 安全性
- ✅ **生产环境**：移除调试日志，避免敏感信息泄露
- ✅ **错误追踪**：保留error日志，便于问题排查

---

## 🎯 9. 执行检查清单

### 清理前检查
- [ ] 备份数据库（`./deployment/backup-database.sh`）
- [ ] 创建Git分支（`git checkout -b code-cleanup-20260209`）
- [ ] 通知团队暂停合并代码

### 清理后验证
- [ ] 运行后端测试（`cd backend && mvn test`）
- [ ] 运行前端构建（`cd frontend && npm run build`）
- [ ] 运行测试脚本（`./test-complete-business-flow.sh`）
- [ ] 手动测试核心流程（订单创建→扫码→结算）
- [ ] 检查小程序编译（微信开发者工具）

### 部署检查
- [ ] Code Review（至少2人）
- [ ] 更新CHANGELOG.md
- [ ] 打Tag（`v3.1.0-cleanup`）
- [ ] 部署到测试环境验证
- [ ] 部署到生产环境

---

## 📝 10. 附录：清理脚本

### 10.1 批量删除废弃API（示例）
```bash
#!/bin/bash
# remove-deprecated-apis.sh

FILES=(
  "backend/src/main/java/com/fashion/supplychain/production/controller/ProductionOrderController.java"
  "backend/src/main/java/com/fashion/supplychain/production/controller/ScanRecordController.java"
  # ... 其他文件
)

for file in "${FILES[@]}"; do
  echo "处理: $file"
  # 删除@Deprecated标记的方法（需手动确认）
  # sed -i '' '/^    @Deprecated$/,/^    }$/d' "$file"
done
```

### 10.2 清理未使用导入（ESLint）
```bash
#!/bin/bash
# remove-unused-imports.sh

cd frontend
npx eslint --fix \
  --rule '@typescript-eslint/no-unused-vars: error' \
  --ext .ts,.tsx \
  src/

npx prettier --write src/**/*.{ts,tsx}
```

### 10.3 清理Console日志
```bash
#!/bin/bash
# clean-console-logs.sh（项目已有）

# PC端
cd frontend
grep -r "console\.log(" src/ | grep -v "console.error" | cut -d: -f1 | sort -u

# 小程序端
cd miniprogram
sed -i '' 's/console\.log(/\/\/ console.log(/g' **/*.js
```

---

## 📞 联系与支持

**问题反馈**：
- 清理过程遇到问题请参考 [开发指南.md](开发指南.md)
- 测试失败请运行 `./check-system-status.sh` 系统诊断
- 数据库问题请参考 [deployment/数据库配置.md](deployment/数据库配置.md)

**相关文档**：
- [快速测试指南.md](快速测试指南.md) - 验证业务流程
- [系统状态.md](系统状态.md) - 系统健康度检查
- [clean-deprecated-code.py](clean-deprecated-code.py) - 自动清理工具

---

**报告结束** - 立即开始清理以提升代码质量！ 🚀
