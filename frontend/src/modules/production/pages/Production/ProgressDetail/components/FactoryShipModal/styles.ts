import React from 'react';

export const thStyle: React.CSSProperties = {
  border: '1px solid var(--color-border-light)',
  padding: '4px 8px',
  textAlign: 'center',
  fontWeight: 600,
  whiteSpace: 'nowrap',
};

export const tdStyle: React.CSSProperties = {
  border: '1px solid var(--color-border-light)',
  padding: '4px 8px',
  textAlign: 'center',
  whiteSpace: 'nowrap',
};

export const tdEditStyle: React.CSSProperties = {
  border: '1px solid var(--color-border-light)',
  padding: '3px 4px',
  minWidth: 64,
};
