# 前端上线前全面检查清单
**生成时间**：2026-02-03  
**检查范围**：前端服务 (React + Vite + TypeScript)

---

## ✅ 一、启动验证（已完成）

### 1.1 服务启动
- [x] 前端服务运行中：http://localhost:5173/
- [x] 环境变量配置正常：`VITE_API_BASE_URL=http://192.168.2.248:8088`
- [x] ESLint/TypeScript 已配置
- [x] 依赖安装完整（241个源文件）

### 1.2 启动问题修复
- [x] 修复 React 导入错误（2个文件）：
  - `src/hooks/useFinancePermission.tsx`
  - `src/hooks/useMemoizedCallback.ts`
- [x] 修复未定义变量错误：`NodeDetailModal.tsx` 中的 `fixedProcessName` 和 `fixedUnitPrice`
- [x] ESLint 检查结果：**0 errors, 29 warnings**

---

## ⚠️ 二、代码质量扫描（发现问题）

### 2.1 垃圾代码扫描
#### ESLint 警告（29个）
**需要清理的未使用变量/导入**：
1. `DashboardStats/index.tsx` - `StatisticProps` 未使用
2. `DictAutoComplete.tsx` - `useEffect` 未使用
3. `LiquidProgressLottie.tsx` - `pulseColor` 未使用
4. `NodeDetailModal.tsx` - 9个未使用的 Ant Design 组件导入
5. `NodeDetailModal.tsx` - 7个未使用的变量
6. `StylePrintModal.tsx` - `Image` 未使用
7. `StyleInfo/index.tsx` - 多个未使用的组件和变量

**整改建议**：
```bash
# 1. 清理未使用的导入（重点文件）
frontend/src/components/common/NodeDetailModal.tsx  # 16个警告
frontend/src/modules/basic/pages/StyleInfo/index.tsx  # 8个警告

# 2. 删除未使用的变量
- pulseColor (LiquidProgressLottie.tsx)
- canDelegateFactory (NodeDetailModal.tsx)
- handlePriceChange (NodeDetailModal.tsx)
```

### 2.2 重复代码扫描（jscpd）
**⚠️ 重复率过高：3.44%（超过1%阈值）**

| 文件类型 | 重复块数 | 重复行数 | 重复率 |
|---------|---------|---------|--------|
| TSX     | 94      | 1403 行 | 4.31%  |
| JavaScript | 41   | 986 行  | 5.53%  |
| TypeScript | 12   | 113 行  | 1.42%  |
| **总计** | **147** | **2502 行** | **3.44%** |

**高风险重复代码示例**：
1. `App.tsx` - 29行重复代码块（993 tokens）
2. `StyleAssets.tsx` ↔ `DataCenter/index.tsx` - 15行重复（192 tokens）
3. `AuthContext.tsx` ↔ `PaymentApproval/index.tsx` - 7行重复（85 tokens）

**整改建议**：
```bash
# 1. 查看详细报告
open frontend/reports/jscpd/html/index.html

# 2. 整改优先级
P0（必须）：重复率>5%的文件（JavaScript文件）
P1（重要）：重复行数>10行的代码块
P2（优化）：重复块数>3个的文件

# 3. 整改方法
- 提取公共组件（如：StyleAssets 中的重复渲染逻辑）
- 提取公共函数（如：权限判断、数据转换）
- 使用 CSS 变量替代重复样式
```

---

## ⚠️ 三、文案检查（发现问题）

### 3.1 TODO 标记（4个，需处理）
| 文件 | 行号 | 内容 | 处理建议 |
|------|------|------|---------|
| `ScanCountChart/index.tsx` | 45 | `// TODO: 替换为真实API` | 实现后端API或标记为已知限制 |
| `FinishedSettlementContent.tsx` | 430 | `// TODO: 后端API开发中` | 补充完成时间或临时方案 |
| `FinishedInventory/index.tsx` | 176 | `// TODO: 调用后端API` | 实现后端API调用 |
| `MaterialPurchase/index.tsx` | 478 | `// TODO: 后续优化` | 确认是否需要当前版本实现 |

### 3.2 占位文案（2个）
| 文件 | 行号 | 问题 | 建议 |
|------|------|------|------|
| `MaterialPurchase/index.tsx` | 829, 1227 | `'未命名'` 作为用户名默认值 | 改为 `'系统用户'` 或 `'操作员'` |

### 3.3 文案一致性检查（待人工核对）
**需要核对的术语**（建议全局搜索确认）：
- [ ] "提交" vs "确认提交" vs "保存"
- [ ] "删除" vs "移除"
- [ ] "订单号" vs "订单编码"
- [ ] "库存预警" vs "库存提醒"
- [ ] "完成" vs "已完成" vs "完成"

**执行命令**：
```bash
cd frontend/src
grep -rn "确认提交\|提交" . --include="*.tsx" | wc -l
grep -rn "删除\|移除" . --include="*.tsx" | wc -l
```

### 3.4 错别字检查（待人工核对）
**重点检查页面**（表单提示、弹窗文案、页面说明）：
- [ ] 生产订单页面（`Production/List/index.tsx` - 2513行）
- [ ] 裁剪管理页面（`Cutting/index.tsx` - 2190行）
- [ ] 财务结算页面（`FinanceCenter/`）
- [ ] 仓库管理页面（`warehouse/`）

---

## 🔧 四、上线前最终验证（待执行）

### 4.1 性能验证
- [ ] 使用 Lighthouse 扫描首页（`chrome://lighthouse`）
- [ ] 首屏加载时间 ≤ 3秒
- [ ] 无性能红项警告
- [ ] 资源加载优化（图片/CSS/JS压缩）

### 4.2 兼容性验证
- [ ] Chrome 最新版验证
- [ ] Firefox 最新版验证
- [ ] Edge 最新版验证
- [ ] Safari 验证（macOS）
- [ ] 移动端 H5 适配验证

### 4.3 安全验证
- [ ] 无硬编码密钥/Token
- [ ] 接口请求带 Authorization token
- [ ] 敏感数据脱敏显示（手机号、身份证）
- [ ] XSS 防护（输入校验）
- [ ] CSRF 防护（Token验证）

### 4.4 日志验证
- [ ] 控制台无 DEBUG 级冗余日志
- [ ] 控制台无报错日志（404/500/语法错）
- [ ] 生产环境禁用 `console.log`

### 4.5 回归测试
**核心流程测试**：
- [ ] 登录 → 访问首页 → 无异常
- [ ] 生产订单创建 → 查询 → 编辑 → 保存
- [ ] 裁剪单管理 → 扫码 → 提交
- [ ] 财务结算 → 审批 → 对账
- [ ] 仓库管理 → 入库 → 出库 → 库存查询

---

## 📊 五、问题汇总与优先级

### P0 - 紧急（必须修复）
1. **重复代码率 3.44%**（超过1%阈值）：147个重复块，2502行重复代码
   - **影响**：维护成本高，潜在Bug风险
   - **工作量**：2-3天（提取公共组件、重构重复逻辑）

2. **4个 TODO 标记**：未完成的后端API调用
   - **影响**：功能不完整，可能影响用户操作
   - **工作量**：0.5-1天（确认后端API状态，实现或标记临时方案）

### P1 - 重要（建议修复）
1. **29个 ESLint 警告**：未使用的变量和导入
   - **影响**：代码冗余，打包体积增大
   - **工作量**：0.5天（自动修复：`npm run lint:fix`）

2. **文案一致性问题**："未命名"占位符、术语不统一
   - **影响**：用户体验差，显得不专业
   - **工作量**：0.5天（全局替换）

### P2 - 优化（有时间处理）
1. **大文件优化**：
   - `Production/List/index.tsx`（2513行）
   - `Cutting/index.tsx`（2190行）
   - **建议**：拆分为独立组件和 Hooks

2. **React 19 依赖冲突**：
   - **警告**：`@react-three/drei` 与 React 18 版本冲突
   - **建议**：考虑升级到 React 19 或使用 `--legacy-peer-deps`

---

## 📝 六、执行计划（3天）

### Day 1：修复P0问题
- 上午：提取公共组件（处理TOP 10重复代码块）
- 下午：实现或标记4个TODO功能

### Day 2：修复P1问题 + 性能验证
- 上午：自动修复ESLint警告，优化文案
- 下午：Lighthouse扫描，性能优化

### Day 3：最终验证 + 回归测试
- 上午：安全验证、日志验证
- 下午：全流程回归测试

---

## ✅ 验收标准

- [ ] 重复代码率 < 1%
- [ ] ESLint 检查：0 errors, 0 warnings
- [ ] 无 TODO 标记（或已标记为已知限制）
- [ ] Lighthouse 性能评分 > 90
- [ ] 核心流程回归测试通过
- [ ] 生产环境无控制台报错
- [ ] 文案无错别字、术语统一

---

**报告生成**：2026-02-03  
**下一步**：执行P0问题修复，优先处理重复代码和TODO标记。
