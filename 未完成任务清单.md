# 未完成任务清单

**创建日期**: 2026-01-29  
**优先级**: P0 = 高 | P1 = 中 | P2 = 低

---

## 🔥 高优先级任务（P0 - 本周完成）

### 1. 代码质量修复（必须）

#### 1.1 后端 TODO 清理（3个）
```java
// ❌ backend/.../ProductionOrderOrchestrator.java:285
// TODO: 实现具体的数据复制逻辑
private ProductionOrder copyOrderData(ProductionOrder source) {
    throw new UnsupportedOperationException("订单复制功能待实现");
}

// ❌ backend/.../OperationLogController.java:45
// TODO: 实现操作日志功能
@GetMapping("/logs")
public ApiResponse<?> getLogs() {
    throw new UnsupportedOperationException("操作日志查询待实现");
}

// ❌ backend/.../AuditLogService.java
// TODO: 实现审计日志服务
```

**优先级**: P0  
**工作量**: 2-4小时  
**影响**: 订单复制功能无法使用

---

#### 1.2 前端 Mock API 替换（3个）
```typescript
// ❌ frontend/src/modules/dashboard/pages/Dashboard/index.tsx:150
// TODO: 替换为真实API
const mockData = {
  todayQuality: { total: 1200, qualified: 1150, defective: 50, rate: 95.83 },
  // ...
};

// ❌ frontend/src/modules/dashboard/components/MiniDataDashboard/index.tsx:45
// TODO: 调用审批核实API
const handleApprove = async (id: string) => {
  console.log('TODO: 调用审批API', id);
};

// ❌ frontend/src/modules/finance/pages/FinanceDashboard/index.tsx
// TODO: 替换为真实财务数据API
```

**优先级**: P0  
**工作量**: 1-2小时  
**影响**: 数据看板显示假数据

---

#### 1.3 调试代码清理
```bash
# 后端调试代码（4处）
backend/.../CuttingTaskController.java:System.out.println("生成菲号: " + bundleNo);
backend/.../QualityController.java:System.out.println("质检结果: " + result);

# 小程序调试代码（~20处，保留关键业务日志）
miniprogram/pages/work/index.js:console.log('扫码结果:', data);
miniprogram/utils/syncManager.js:console.log('同步成功:', res);
```

**优先级**: P1  
**工作量**: 30分钟  
**影响**: 生产环境日志污染

---

### 2. 垃圾文件清理（必须）

```bash
# 备份文件（11个）
miniprogram/pages/work/index.js.bak
miniprogram/pages/work/index.js.bak2
miniprogram/pages/work/index.js.bak3
# ... 等

# 日志文件（36.6MB）
logs/backend-*.log
logs/eslint-*.txt

# 执行清理
./clean-system.sh
```

**优先级**: P0  
**工作量**: 10分钟  
**影响**: 项目体积过大

---

## 📊 中优先级任务（P1 - 本月完成）

### 3. 功能完善

#### 3.1 PC端实时同步（可选）
```typescript
// 方案1：WebSocket
const ws = new WebSocket('ws://localhost:8080/sync');
ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  updateLocalState(data);
};

// 方案2：轮询（30秒）
setInterval(() => {
  fetchLatestData();
}, 30000);
```

**优先级**: P1  
**工作量**: 4-8小时  
**影响**: PC端需手动刷新查看最新数据

---

#### 3.2 成品结算审批API对接
```typescript
// ❌ 当前状态：前端已完成，后端未实现
POST /api/finished-settlement/approve

// ✅ 需要实现：
// 1. 后端Controller
// 2. Service层业务逻辑
// 3. 权限验证
```

**优先级**: P1  
**工作量**: 2-3小时  
**影响**: 成品结算流程不完整

---

#### 3.3 操作日志系统
```java
// 需要实现：
// 1. OperationLog实体
// 2. OperationLogMapper
// 3. OperationLogService
// 4. 自动记录（AOP拦截）

@Aspect
public class OperationLogAspect {
    @Around("@annotation(LogOperation)")
    public Object logOperation(ProceedingJoinPoint joinPoint) {
        // 记录操作前后状态
    }
}
```

**优先级**: P1  
**工作量**: 4-6小时  
**影响**: 无法追溯用户操作历史

---

### 4. 卡片视图集成（可选）

**已完成**：
- ✅ ProductionList（我的订单）- 6列
- ✅ ProgressDetail（生产进度）- 6列
- ✅ PatternProduction（样板生产）- 6列
- ✅ StyleInfo（款式资料）- 6列

**待集成**（可选）：
```typescript
// ❌ OrderManagement（订单管理）
// ❌ DataCenter（数据中心）
// ❌ MaterialPurchase（物料采购）
// ❌ Cutting（裁剪单）

// 模板（参考archive/2026-01-28-完成报告/卡片视图快速集成指南.md）
const [viewMode, setViewMode] = useState<'table' | 'card'>('table');

<UniversalCardView.ViewToggle value={viewMode} onChange={setViewMode} />
{viewMode === 'card' ? <UniversalCardView /> : <Table />}
```

**优先级**: P2  
**工作量**: 2-4小时  
**影响**: 无，纯视觉优化

---

## 🔧 低优先级任务（P2 - 有空再做）

### 5. 性能优化

#### 5.1 依赖升级
```json
{
  "eslint": "^8.57.0" → "^9.0.0",
  "glob": "^10.3.10" → "^11.0.0",
  "rimraf": "^5.0.5" → "^6.0.0"
}
```

**优先级**: P2  
**工作量**: 1-2小时  
**影响**: 无，版本较新

---

#### 5.2 前端按需加载优化
```typescript
// 当前：路由懒加载已完成
const Dashboard = lazy(() => import('./modules/dashboard'));

// 可优化：组件级懒加载
const HeavyComponent = lazy(() => import('./HeavyComponent'));
```

**优先级**: P2  
**工作量**: 2-3小时  
**影响**: 首屏加载略慢（可接受）

---

#### 5.3 小程序分包加载
```json
{
  "subpackages": [
    {
      "root": "pages/production",
      "pages": ["scan", "quality", "warehousing"]
    }
  ]
}
```

**优先级**: P2  
**工作量**: 3-4小时  
**影响**: 小程序包体积<2MB（未超限）

---

## 📋 任务执行计划

### 本周（2026-01-29 ~ 2026-02-02）

**Day 1 (周三)**：
1. ✅ 文档整合（已完成）
2. [ ] 垃圾文件清理（10分钟）
3. [ ] 后端TODO清理 - 订单复制逻辑（2小时）

**Day 2 (周四)**：
1. [ ] 前端Mock API替换（2小时）
2. [ ] 调试代码清理（30分钟）

**Day 3 (周五)**：
1. [ ] 后端TODO清理 - 操作日志功能（4小时）

**Day 4-5 (周末)**：
1. [ ] PC端实时同步（可选，8小时）
2. [ ] 成品结算审批API（可选，3小时）

---

### 本月（2026-02）

**Week 1**：
- [ ] 完成所有P0任务

**Week 2**：
- [ ] 完成所有P1任务
- [ ] 卡片视图集成（可选）

**Week 3-4**：
- [ ] 性能优化（P2任务）
- [ ] 系统测试和上线准备

---

## ✅ 完成标准

### 代码质量
- [ ] 无TODO注释
- [ ] 无Mock数据
- [ ] 无System.out.println
- [ ] Console.log仅保留关键业务日志

### 文件清理
- [ ] 无.bak备份文件
- [ ] 日志文件<10MB
- [ ] 无垃圾文件

### 功能完整性
- [ ] 订单复制功能可用
- [ ] 操作日志可查询
- [ ] 数据看板显示真实数据
- [ ] 成品结算流程完整

---

## 🎯 下一步行动

**立即执行**（10分钟）：
```bash
# 1. 清理垃圾文件
./clean-system.sh

# 2. 查看文件大小
du -sh logs/

# 3. 删除备份文件
find miniprogram -name "*.bak*" -delete
```

**今日完成**（2小时）：
1. 实现订单复制逻辑
2. 替换主页Mock数据

**本周完成**（4小时）：
1. 操作日志功能
2. 调试代码清理

---

**记住**：P0任务必须本周完成，P1任务本月完成，P2任务有空再做。

