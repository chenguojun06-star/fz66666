# 微信小程序 - 业务逻辑优化工具完整指南

**最后更新**: 2026-01-25  
**项目**: 服装供应链管理系统 - 小程序端  
**目标**: 提升代码质量、优化业务逻辑、减少Bug、提高可维护性

---

## 📋 目录

1. [已完成的优化](#已完成的优化)
2. [推荐工具清单](#推荐工具清单)
3. [业务逻辑优化工具](#业务逻辑优化工具)
4. [快速安装指南](#快速安装指南)
5. [最佳实践](#最佳实践)

---

## ✅ 已完成的优化

### 扫码任务与通知链路一致性（2026-04-09）
- ✅ 采购任务、裁剪分扎详情、质检录入页补齐“颜色 × 码数 × 数量”矩阵布局；裁剪任务列表继续保持原逐任务分码展示
- ✅ 共享解析入口固定为 `miniprogram/utils/orderParser.js`，页面侧统一读取 `sizeMatrix.sizes / rows / totalQuantity`
- ✅ 小云通知页点击后可按通知类型直达采购、裁剪、质检或扫描入口，不再只做已读处理
- ✅ 裁剪任务列表保持原按颜色/尺码逐任务展示，避免自动分码任务入口被聚合吞掉
- ✅ 采购扫码新增快通道：有真实采购任务时，扫码后直接领取并进入采购详情填写回料，不再先进入通用扫码确认页

**后续开发约定**：
- 新增手机端任务页时，若页面需要展示订单颜色/尺码分布，必须复用 `buildColorSizeMatrix` 或 `buildColorSizeMatrixFromOrder`，禁止再次手写 `colorGroups / allSizes` 之类页面私有拼装逻辑。
- 小云通知若新增任务型 `noticeType`，必须同步补齐 `pages/work/inbox/index.js` 的 `_openNoticeAction()` 路由规则，保证通知可以直接落到可执行页面。
- 裁剪任务页属于例外：后端已按颜色/尺码拆任务，小程序列表必须保留逐任务展示，禁止再按订单聚合覆盖现场分码入口。

### 代码质量（2026-01-25）
- ✅ ESLint 警告：40个 → **0个** (↓100%)
- ✅ wx.showToast：41处 → **2处**(注释) (↓95%)
- ✅ 创建工具库：`uiHelper.js`、`validator.js`
- ✅ 代码重复率：降低 ~30%
- ✅ 严格相等检查：`==` → `===`（37处修复）

### 已配置工具 ✅
| 工具 | 功能 | 命令 |
|------|------|------|
| ESLint | 代码质量检查 | `npm run 检查` |
| ESLint --fix | 自动修复 | `npm run 修复` |
| TypeScript | JSDoc类型检查 | `npm run 类型检查` |
| 微信开发者工具 | 官方IDE+调试 | 打开项目 |

---

## 🎯 推荐工具清单

### 🔥 P0 - 立即可用（无需安装）

#### 1. **微信开发者工具 - 内置分析器** ⭐⭐⭐⭐⭐

**① 代码质量检查**
```
菜单栏 → 工具 → 代码质量
```
**功能**：
- ✅ 检测未使用的变量和函数
- ✅ 检测未定义的变量（拼写错误）
- ✅ 检测语法错误
- ✅ 检测潜在的性能问题
- ✅ 检测废弃API使用

**示例输出**：
```
⚠️ pages/scan/index.js:156 - 'oldData' 已声明但未使用
⚠️ pages/work/index.js:89 - 'productionProgress' 已声明但未使用
⚠️ utils/helper.js:23 - 使用了废弃API 'wx.showNavigationBarLoading'
```

**② 性能分析器（Performance）**
```
调试器 → Performance 面板
```
**功能**：
- 📊 录制页面加载性能
- 📊 分析渲染耗时（setData）
- 📊 检测内存泄漏
- 📊 查看网络请求瀑布图
- 📊 识别长任务（>50ms）

**使用步骤**：
1. 打开 Performance 面板
2. 点击"开始录制"
3. 操作小程序（如扫码、提交）
4. 停止录制，查看火焰图
5. 查找耗时最长的函数调用

**③ 代码依赖分析**
```
工具 → 依赖分析
```
**功能**：
- 🔍 分析文件依赖关系
- 🔍 检测循环依赖（导致加载卡顿）
- 🔍 查看代码体积占比
- 🔍 识别重复引用

**④ 体积分析**
```
详情 → 本地设置 → 代码质量 → 上传代码时进行代码保护
详情 → 基本信息 → 查看代码包大小
```
**限制**：主包 ≤ 2MB，总体积 ≤ 20MB

#### 2. **真机调试** ⭐⭐⭐⭐⭐
```
工具栏 → 真机调试
```
**功能**：
- 📱 在真实设备上测试性能
- 📱 检查网络请求（真实网络环境）
- 📱 调试扫码功能（摄像头）
- 📱 验证用户体验（滚动、点击流畅度）
- 📱 检测机型兼容性（iOS vs Android）

#### 3. **AppData 监控**
```
调试器 → AppData 面板
```
**功能**：
- 实时查看 `data` 数据变化
- 监控 `setData` 调用频率
- 检测数据大小（避免 setData 过大）

---

### ⭐ P1 - 强烈推荐安装

#### 1. **Prettier - 代码格式化** ⭐⭐⭐⭐⭐

**安装**：
```bash
cd miniprogram
npm install -D prettier

# 创建配置文件
cat > .prettierrc << 'EOF'
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "endOfLine": "lf"
}
EOF

# 添加脚本
npm pkg set scripts.格式化="prettier --write \"**/*.{js,json,wxml,wxss}\""
npm pkg set scripts.检查格式="prettier --check \"**/*.{js,json,wxml,wxss}\""
```

**使用**：
```bash
# 格式化所有文件
npm run 格式化

# 仅检查格式（不修改）
npm run 检查格式

# 格式化单个文件
npx prettier --write pages/scan/index.js
```

#### 2. **代码复杂度检查** ⭐⭐⭐⭐

**配置 ESLint 复杂度规则**（已有 ESLint）：
```bash
# 编辑 miniprogram/package.json，在 eslintConfig.rules 中添加
```

```json
{
  "eslintConfig": {
    "rules": {
      "complexity": ["warn", 15],
      "max-depth": ["warn", 4],
      "max-lines-per-function": ["warn", {
        "max": 150,
        "skipBlankLines": true,
        "skipComments": true
      }],
      "max-params": ["warn", 5],
      "max-nested-callbacks": ["warn", 3]
    }
  }
}
```

**检查命令**：
```bash
npm run 检查
```

**示例输出**：
```
pages/scan/index.js
  245:3  warning  Function 'processScanCode' has a complexity of 18  complexity
  450:3  warning  Function 'onConfirmScan' is too long (165 lines)  max-lines-per-function
```

#### 3. **依赖检查 - depcheck** ⭐⭐⭐⭐

**安装**：
```bash
npm install -g depcheck
```

**使用**：
```bash
cd miniprogram
depcheck
```

**输出**：
```
Unused dependencies
* @types/node (仅开发时使用，可忽略)

Missing dependencies
* lodash (代码中使用但未安装，需安装)

Unused devDependencies
* jest (已删除测试，可卸载)
```

#### 4. **自动导入管理 - VS Code 扩展** ⭐⭐⭐⭐

**安装 VS Code 扩展**：
```bash
# 小程序开发支持
code --install-extension qiu8310.minapp-vscode

# 自动导入整理
code --install-extension dbaeumer.vscode-eslint
```

**功能**：
- 自动补全小程序API（wx.*）
- 自动导入未定义的模块
- WXML/WXSS 语法高亮
- 智能提示

---

### 📊 P2 - 业务逻辑优化工具

#### 1. **代码覆盖率检查 - 手动分析** ⭐⭐⭐⭐

**检查未使用的函数**：
```bash
cd miniprogram

# 1. 查找所有函数定义
grep -rh "^\s*[a-zA-Z_][a-zA-Z0-9_]*\s*(" pages/ --include="*.js" | \
  sed 's/(.*//' | sed 's/^\s*//' | sort -u > /tmp/functions.txt

# 2. 查找WXML中的事件绑定
grep -rh "bind[a-z]*=\"" pages/ --include="*.wxml" | \
  grep -o 'bind[a-z]*="[^"]*"' | cut -d'"' -f2 | sort -u > /tmp/bindings.txt

# 3. 对比找出未使用的函数（需手动检查）
```

**检查废弃代码（注释掉但未删除）**：
```bash
# 查找被注释的代码块
grep -rn "^[\s]*//.*{" pages/ --include="*.js" | head -20
```

#### 2. **业务逻辑重复检测 - jsinspect** ⭐⭐⭐

**安装**：
```bash
npm install -g jsinspect
```

**使用**：
```bash
cd miniprogram

# 检测重复代码（阈值30行）
jsinspect pages/ -t 30

# 输出示例：
# Match - 2 instances
# pages/scan/index.js:245-278
# pages/work/index.js:156-189
```

**修复策略**：提取为公共函数到 `utils/` 目录

#### 3. **循环依赖检测 - madge** ⭐⭐⭐⭐

**安装**：
```bash
npm install -g madge
```

**使用**：
```bash
cd miniprogram

# 检测循环依赖
madge --circular pages/

# 生成依赖图（需安装 Graphviz）
madge --image deps.svg pages/
```

**示例输出**：
```
Circular dependencies:
pages/scan/index.js > utils/api.js > utils/request.js > pages/scan/index.js
```

#### 4. **Git 提交质量分析** ⭐⭐⭐

**统计最常修改的文件（热点文件，可能有问题）**：
```bash
cd /Users/guojunmini4/Documents/服装66666

# 最近30天修改最多的文件
git log --since="30 days ago" --name-only --pretty=format: miniprogram/ | \
  sort | uniq -c | sort -rn | head -20
```

**示例输出**：
```
  45 miniprogram/pages/scan/index.js     # 频繁修改，需重构
  23 miniprogram/utils/api.js
  18 miniprogram/pages/work/index.js
```

#### 5. **业务逻辑文档生成 - JSDoc** ⭐⭐⭐⭐

**安装**：
```bash
cd miniprogram
npm install -D jsdoc
```

**配置 jsdoc.json**：
```json
{
  "source": {
    "include": ["pages", "utils"],
    "includePattern": ".+\\.js$"
  },
  "opts": {
    "destination": "./docs",
    "recurse": true,
    "readme": "README.md"
  }
}
```

**生成文档**：
```bash
npx jsdoc -c jsdoc.json
```

**功能**：
- 从JSDoc注释生成HTML文档
- 列出所有函数、参数、返回值
- 帮助理解业务逻辑

---

### 🔬 P3 - 高级工具（按需使用）

#### 1. **单元测试 - Jest** ⭐⭐⭐

**安装**：
```bash
cd miniprogram
npm install -D jest jest-environment-miniprogram
```

**示例测试（utils/validator.js）**：
```javascript
// utils/__tests__/validator.test.js
const { validateFields, validators } = require('../validator');

describe('validator', () => {
  test('validateFields - 空值检测', () => {
    const rules = [
      { field: 'username', value: '', message: '请输入用户名' }
    ];
    expect(validateFields(rules, false)).toBe(false);
  });

  test('validators.positive - 正数校验', () => {
    expect(validators.positive(10)).toBe(true);
    expect(validators.positive(-5)).toBe(false);
    expect(validators.positive(0)).toBe(false);
  });
});
```

**运行测试**：
```bash
npm test
```

#### 2. **性能监控脚本** ⭐⭐⭐

**创建 scripts/analyze-performance.js**：
```javascript
const fs = require('fs');
const path = require('path');

// 分析所有 setData 调用（大数据传输会导致卡顿）
function analyzeSetData() {
  const files = getAllJsFiles('pages');
  const issues = [];

  files.forEach(file => {
    const content = fs.readFileSync(file, 'utf-8');
    const lines = content.split('\n');

    lines.forEach((line, idx) => {
      // 检测 setData 传入大对象
      if (line.includes('setData') && line.length > 200) {
        issues.push({
          file: file.replace(process.cwd(), ''),
          line: idx + 1,
          issue: 'setData可能传输数据过大',
          code: line.trim().slice(0, 100)
        });
      }
    });
  });

  return issues;
}

function getAllJsFiles(dir) {
  // 递归获取所有JS文件...
}

console.log('性能问题：', analyzeSetData());
```

**运行**：
```bash
node scripts/analyze-performance.js
```

#### 3. **代码审查清单生成器** ⭐⭐⭐

**创建 scripts/generate-review-checklist.sh**：
```bash
#!/bin/bash
cd miniprogram

echo "📋 代码审查清单（自动生成）"
echo ""

echo "1️⃣ 代码质量问题："
npm run 检查 2>&1 | grep "warning" | wc -l | xargs echo "  - ESLint警告数："

echo ""
echo "2️⃣ 类型检查问题："
npm run 类型检查 2>&1 | grep "error TS" | wc -l | xargs echo "  - TypeScript错误数："

echo ""
echo "3️⃣ 潜在Bug（未使用变量）："
grep -rn "const [a-zA-Z_]* =" pages/ --include="*.js" | wc -l | xargs echo "  - 变量声明数："
eslint pages/ 2>&1 | grep "no-unused-vars" | wc -l | xargs echo "  - 未使用变量数："

echo ""
echo "4️⃣ 代码复杂度："
wc -l pages/**/*.js | tail -1 | awk '{print "  - 总代码行数：" $1}'

echo ""
echo "5️⃣ 依赖问题："
depcheck 2>&1 | grep "Unused" -A 5

echo ""
echo "✅ 审查完成"
```

**运行**：
```bash
chmod +x scripts/generate-review-checklist.sh
./scripts/generate-review-checklist.sh
```

---

## 🚀 快速安装指南

### 方案A：核心工具（推荐新手）

```bash
cd miniprogram

# 1. 安装 Prettier
npm install -D prettier
echo '{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}' > .prettierrc

# 2. 添加脚本
npm pkg set scripts.格式化="prettier --write \"**/*.{js,json,wxml,wxss}\""
npm pkg set scripts.检查格式="prettier --check \"**/*.{js,json,wxml,wxss}\""

# 3. 全局安装依赖检查
npm install -g depcheck

# 4. 使用
npm run 格式化
depcheck
```

### 方案B：完整工具链（推荐进阶）

```bash
cd miniprogram

# 1. 安装所有工具
npm install -D prettier jsdoc jest jest-environment-miniprogram
npm install -g depcheck jsinspect madge

# 2. 添加复杂度规则到 package.json
# （需手动编辑 eslintConfig.rules，添加上述规则）

# 3. 创建文档配置
echo '{
  "source": {"include": ["pages", "utils"]},
  "opts": {"destination": "./docs", "recurse": true}
}' > jsdoc.json

# 4. 添加所有脚本
npm pkg set scripts.格式化="prettier --write \"**/*.{js,json,wxml,wxss}\""
npm pkg set scripts.检查格式="prettier --check \"**/*.{js,json,wxml,wxss}\""
npm pkg set scripts.生成文档="jsdoc -c jsdoc.json"
npm pkg set scripts.检测重复="jsinspect pages/ -t 30"
npm pkg set scripts.检测循环依赖="madge --circular pages/"

# 5. 运行完整检查
npm run 全面检查
depcheck
npm run 检测重复
npm run 检测循环依赖
```

---

## 📖 业务逻辑优化最佳实践

### 1. 每日检查清单

**早上提交前**（5分钟）：
```bash
cd miniprogram

# 1. 自动修复代码规范
npm run 修复

# 2. 格式化代码
npm run 格式化

# 3. 类型检查
npm run 类型检查

# 4. 检查依赖
depcheck
```

**周五发布前**（15分钟）：
```bash
# 1. 完整检查
npm run 全面检查

# 2. 检测重复代码
npm run 检测重复

# 3. 检测循环依赖
npm run 检测循环依赖

# 4. 真机测试
# 使用微信开发者工具 → 真机调试 → 测试核心流程
```

### 2. 代码审查要点

**业务逻辑检查**：
- [ ] 是否有重复的业务逻辑代码？→ 提取到工具函数
- [ ] 函数是否过长（>150行）？→ 拆分为小函数
- [ ] 圈复杂度是否过高（>15）？→ 简化if-else逻辑
- [ ] 是否有硬编码的业务规则？→ 提取为配置

**性能检查**：
- [ ] `setData` 是否传输大数据？→ 只传必要数据
- [ ] 是否有频繁的 `setData` 调用？→ 批量更新
- [ ] 网络请求是否有防抖节流？→ 添加防重复点击
- [ ] 列表渲染是否使用 `wx:key`？→ 提升性能

**错误处理**：
- [ ] API 调用是否有 try-catch？
- [ ] 是否有用户友好的错误提示？
- [ ] 是否有错误日志记录？

### 3. 重构优先级

**P0 - 立即重构**：
- ❌ 重复代码超过50行
- ❌ 函数复杂度 >20
- ❌ 函数行数 >200行
- ❌ 循环依赖
- ❌ 未处理的异常

**P1 - 本周重构**：
- ⚠️ 重复代码30-50行
- ⚠️ 函数复杂度 15-20
- ⚠️ 函数行数 150-200行
- ⚠️ 未使用的变量/函数
- ⚠️ 硬编码的业务规则

**P2 - 本月优化**：
- 📝 缺少注释的复杂逻辑
- 📝 函数参数过多（>5个）
- 📝 嵌套深度过深（>4层）
- 📝 魔法数字（未定义的常量）

### 4. 工具使用频率建议

| 工具 | 频率 | 时机 |
|------|------|------|
| `npm run 检查` | 每次提交前 | 发现问题 |
| `npm run 修复` | 每次提交前 | 自动修复 |
| `npm run 格式化` | 每次提交前 | 统一格式 |
| `npm run 类型检查` | 每天1次 | 下班前 |
| 真机调试 | 每个功能完成 | 验证体验 |
| 性能分析 | 每周1次 | 周五下午 |
| 依赖检查 | 每月1次 | 月初 |
| 重复代码检测 | 每月1次 | 月初 |

---

## 🎓 学习资源

### 官方文档
- [微信小程序开发文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- [性能优化指南](https://developers.weixin.qq.com/miniprogram/dev/framework/performance/)
- [代码质量指南](https://developers.weixin.qq.com/miniprogram/dev/framework/quality/)

### 工具文档
- [ESLint 规则](https://eslint.org/docs/rules/)
- [Prettier 配置](https://prettier.io/docs/en/options.html)
- [JSDoc 语法](https://jsdoc.app/)

### 最佳实践
- [微信小程序最佳实践](https://developers.weixin.qq.com/community/develop/article/doc/000e2a6ee0c168e7b5589d7745b813)
- [JavaScript 代码整洁之道](https://github.com/ryanmcdermott/clean-code-javascript)

---

## 📞 常见问题

### Q1: 工具太多，从哪开始？
**A**: 从P0工具开始：
1. 使用微信开发者工具的"代码质量"检查（无需安装）
2. 安装 Prettier 格式化代码
3. 每天运行 `npm run 检查` 和 `npm run 修复`

### Q2: ESLint 警告太多怎么办？
**A**: 分批修复：
1. 先修复 `--fix` 可以自动修复的
2. 再修复简单的（如未使用变量）
3. 最后修复复杂的（如重构）

### Q3: 如何评估代码质量？
**A**: 关注4个指标：
1. **ESLint警告数**：目标 <5个
2. **函数平均行数**：目标 <100行
3. **重复代码率**：目标 <10%
4. **圈复杂度**：目标 <15

### Q4: 性能分析发现卡顿，如何优化？
**A**: 3步排查：
1. 检查 `setData` 大小（目标 <64KB）
2. 检查 `setData` 频率（目标 <60次/秒）
3. 检查长任务（目标 <50ms）

---

**总结**：工具只是辅助，核心是养成良好的编码习惯。建议先从 P0 工具开始，逐步引入其他工具。持续优化代码质量，每周进步一点点！ 🚀

# 微信小程序 - 开发工具与规范指南

**最后更新**: 2026-01-24  
**项目**: 服装供应链管理系统 - 小程序端

---

## 📋 目录

1. [工具清单](#工具清单)
2. [代码质量检查](#代码质量检查)
3. [代码优化工具](#代码优化工具)
4. [Bug 检查方法](#bug检查方法)
5. [性能优化](#性能优化)
6. [开发规范](#开发规范)
7. [常见问题自查](#常见问题自查)

---

## 🛠️ 工具清单

### ✅ 已配置工具

| 工具 | 功能 | 命令 | 配置文件 |
|------|------|------|----------|
| **ESLint** | JavaScript 代码质量检查 | `npm run 检查` | package.json |
| **TypeScript** | JSDoc 类型检查 | `npm run 类型检查` | tsconfig.json |
| **微信开发者工具** | 官方 IDE + 调试 | 打开项目 | project.config.json |
| **Design Token** | 统一设计变量 | - | styles/design-tokens.wxss |

### 🎯 推荐补充工具

#### P0 - 立即可用（无需安装）

**1. 微信开发者工具 - 内置功能** ⭐⭐⭐⭐⭐

**代码质量检查**:
```
菜单栏 → 工具 → 代码质量
功能：
- 检测未使用的变量
- 检测未定义的变量
- 检测语法错误
- 检测潜在的性能问题
```

**性能分析**:
```
调试器 → Performance
功能：
- 录制页面加载性能
- 分析渲染耗时
- 检测内存泄漏
- 查看网络请求瀑布图
```

**代码依赖分析**:
```
工具 → 依赖分析
功能：
- 分析文件依赖关系
- 检测循环依赖
- 查看代码体积占比
```

**体积分析**:
```
详情 → 本地设置 → 代码质量 → 上传代码时进行代码保护
详情 → 基本信息 → 查看代码包大小
```

**2. 真机调试** ⭐⭐⭐⭐⭐
```
工具栏 → 真机调试
功能：
- 在真实设备上测试性能
- 检查网络请求
- 调试扫码功能（摄像头）
- 验证用户体验
```

#### P1 - 推荐安装

**1. VS Code 扩展 - 微信小程序支持**
```bash
# WXML、WXSS 语法高亮
code --install-extension qiu8310.minapp-vscode

# 小程序代码片段
code --install-extension wechat-miniprogram.vscode-wxml
```

**2. Prettier（格式化）**
```bash
cd miniprogram
npm install -D prettier

# .prettierrc
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5"
}
```

---

## 🔍 代码质量检查

### 1. ESLint 检查（已配置 ✅）

**基础检查**:
```bash
cd miniprogram

# 检查所有问题
npm run 检查

# 自动修复（新增 ✨）
npm run 修复

# 检查单个文件
npm run 检查 pages/scan/index.js

# 同时运行检查 + 类型检查（新增 ✨）
npm run 全面检查
```

**已启用的规则**:
```javascript
{
  "no-unused-vars": "warn",      // 未使用的变量（警告）
  "no-console": "off",           // 允许 console
  "no-debugger": "warn",         // debugger 警告
  "no-empty": "warn",            // 空代码块
  "no-unreachable": "warn",      // 不可达代码
  "no-undef": "error",           // 未定义变量（错误）
  "eqeqeq": "warn",              // 强制使用 ===
  "curly": "warn",               // 强制使用花括号
  "no-var": "warn",              // 禁止 var，使用 let/const
  "prefer-const": "warn"         // 优先使用 const
}
```

**示例输出**:
```
/pages/scan/index.js
  45:7   warning  Unexpected var, use let or const instead  no-var
  78:12  warning  'oldData' is assigned a value but never used  no-unused-vars
  156:3  warning  Expected { after 'if' condition  curly
  
✖ 3 warnings (0 errors)
  2 warnings potentially fixable with --fix
```

### 2. TypeScript 类型检查

**JSDoc 注释示例**:
```javascript
/**
 * 处理扫码结果
 * @param {string} codeStr - 扫码内容
 * @param {object} options - 扫码选项
 * @param {string} options.scanType - 扫码类型
 * @param {number} [options.quantity] - 可选数量
 * @returns {Promise<object>} 扫码结果
 */
async processScanCode(codeStr, options) {
  // ...
}
```

**运行检查**:
```bash
npm run 类型检查
```

**常见错误**:
```
pages/scan/index.js:45:7 - error TS2339: Property 'xxx' does not exist on type 'object'.
pages/work/index.js:78:12 - error TS7006: Parameter 'e' implicitly has an 'any' type.
```

---

## 🚀 代码优化工具

### 1. 自动格式化（推荐安装）

**安装 Prettier**:
```bash
cd miniprogram
npm install -D prettier

# 创建配置文件
cat > .prettierrc << 'EOF'
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
EOF
```

**添加格式化命令**:
```json
// package.json
{
  "scripts": {
    "格式化": "prettier --write \"**/*.{js,json,wxml,wxss}\"",
    "检查格式": "prettier --check \"**/*.{js,json,wxml,wxss}\""
  }
}
```

**使用**:
```bash
# 格式化所有文件
npm run 格式化

# 仅检查格式（不修改）
npm run 检查格式

# 格式化单个文件
npx prettier --write pages/scan/index.js
```

### 2. 代码复杂度检查

**使用 ESLint 复杂度规则**:
```json
// package.json - eslintConfig
{
  "rules": {
    "complexity": ["warn", 15],         // 圈复杂度不超过15
    "max-depth": ["warn", 4],           // 最大嵌套深度4层
    "max-lines-per-function": ["warn", {
      "max": 150,                       // 函数最多150行
      "skipBlankLines": true,
      "skipComments": true
    }],
    "max-params": ["warn", 5]           // 最多5个参数
  }
}
```

### 3. 依赖分析

**安装 depcheck**:
```bash
npm install -g depcheck

# 运行检查
cd miniprogram
depcheck
```

**输出示例**:
```
Unused dependencies
* @types/node (只在开发时使用)

Missing dependencies
* lodash (在代码中引用但未安装)
```

---

## 🐛 Bug 检查方法

### 1. 常见 Bug 类型自查清单

#### ✅ 数据绑定问题
```javascript
// ❌ 错误：直接修改 data
this.data.list.push(item);

// ✅ 正确：使用 setData
this.setData({
  list: [...this.data.list, item]
});
```

**检查命令**:
```bash
# 搜索直接修改 data 的代码
grep -rn "this.data\." pages/ --include="*.js" | grep -v "this.data\.[a-zA-Z]*)" | grep "="
```

#### ✅ 事件绑定缺失
```wxml
<!-- ❌ 错误：方法名拼写错误 -->
<button bindtap="onReceiveOnlyTypo">领取任务</button>

<!-- ✅ 正确：检查 JS 中是否有对应方法 -->
<button bindtap="onReceiveOnly">领取任务</button>
```

**检查方法**:
```bash
# 1. 提取 WXML 中的所有事件绑定
grep -rh "bind[a-z]*=\"" pages/ --include="*.wxml" | \
  grep -o 'bind[a-z]*="[^"]*"' | \
  cut -d'"' -f2 | sort -u > /tmp/wxml_events.txt

# 2. 在对应的 JS 文件中查找是否定义
# (手动检查，或使用脚本自动化)
```

#### ✅ API 调用错误
```javascript
// ❌ 错误：忘记 await
const result = api.production.executeScan(params);
result.data  // undefined，因为是 Promise

// ✅ 正确：使用 await
const result = await api.production.executeScan(params);
result.data  // 正常
```

**检查命令**:
```bash
# 搜索可能缺少 await 的 API 调用
grep -rn "api\." pages/ --include="*.js" | grep -v "await" | grep -v "then"
```

#### ✅ 变量未定义
```javascript
// ❌ 错误：使用了未定义的变量
if (isMobile) { ... }  // isMobile 未定义

// ✅ 正确：先定义再使用
const isMobile = wx.getSystemInfoSync().platform === 'android';
if (isMobile) { ... }
```

**检查方法**: 使用 ESLint 的 `no-undef` 规则（已启用 ✅）

### 2. 微信开发者工具调试

**Console 调试**:
```javascript
// 1. 基础调试
console.log('变量值:', this.data.scanConfirm);

// 2. 对象深拷贝输出（避免引用问题）
console.log('深拷贝:', JSON.parse(JSON.stringify(this.data)));

// 3. 条件断点
if (this.data.orderNo === 'PO20260122001') {
  debugger;  // 只在特定订单时中断
}
```

**Network 调试**:
```
1. 打开调试器 → Network
2. 筛选 XHR 请求
3. 查看请求参数、响应数据
4. 检查 HTTP 状态码
```

**Storage 调试**:
```
调试器 → Storage
- 查看 localStorage 数据
- 清除缓存数据
- 检查 Token 是否过期
```

### 3. 真机调试必查项

**扫码功能**:
```
⚠️ 模拟器无法测试摄像头，必须真机调试！

1. 点击"预览"或"真机调试"
2. 扫描二维码
3. 在真机上测试扫码领取任务
4. 观察 console 输出
```

**网络请求**:
```
1. 真机调试模式下，打开 vConsole
2. 切换到 Network 标签
3. 检查请求是否成功
4. 查看响应时间
```

---

## ⚡ 性能优化

### 1. 性能分析

**使用微信开发者工具**:
```
1. 调试器 → Performance → 点击录制
2. 操作页面（如打开弹窗、加载列表）
3. 停止录制
4. 分析瀑布图：
   - 蓝色：网络请求
   - 黄色：脚本执行
   - 紫色：渲染
```

**关键指标**:
- **首屏渲染时间**: < 1s
- **setData 调用频率**: < 100次/秒
- **单次 setData 数据量**: < 1MB
- **页面内存占用**: < 50MB

### 2. 常见性能问题

#### ❌ 问题1：频繁 setData
```javascript
// ❌ 错误：循环中调用 setData
for (let i = 0; i < 100; i++) {
  this.setData({
    [`list[${i}]`]: newItem
  });
}

// ✅ 正确：批量更新
const updates = {};
for (let i = 0; i < 100; i++) {
  updates[`list[${i}]`] = newItem;
}
this.setData(updates);
```

#### ❌ 问题2：setData 数据量过大
```javascript
// ❌ 错误：传递整个大对象
this.setData({
  historyList: largeArray  // 1000+ 条数据
});

// ✅ 正确：分页加载
this.setData({
  historyList: largeArray.slice(0, 20)  // 只加载前20条
});
```

#### ❌ 问题3：长列表渲染
```wxml
<!-- ❌ 错误：一次渲染1000条 -->
<view wx:for="{{allRecords}}" wx:key="id">...</view>

<!-- ✅ 正确：使用虚拟列表或分页 -->
<view wx:for="{{visibleRecords}}" wx:key="id">...</view>
```

### 3. 代码体积优化

**检查代码包大小**:
```
详情 → 基本信息 → 代码包信息
主包: < 2MB
总包: < 20MB
```

**分包加载**（如需要）:
```json
// app.json
{
  "pages": ["pages/home/index"],
  "subPackages": [
    {
      "root": "pages/admin",
      "pages": ["index", "users"]
    }
  ]
}
```

---

## 📏 开发规范

### 1. Design Token 使用规范 ⭐⭐⭐⭐⭐

**统一设计变量** (`styles/design-tokens.wxss`):
```wxss
/* ✅ 使用 Design Token */
.button {
  background: var(--color-primary);
  border-radius: var(--radius-md);
  padding: var(--spacing-sm) var(--spacing-md);
}

/* ❌ 禁止硬编码 */
.button {
  background: linear-gradient(135deg, #3b82f6, #2563eb);
  border-radius: 12px;
  padding: 8px 16px;
}
```

**检查硬编码**:
```bash
# 检查硬编码颜色（应该为0）
grep -rn "#[0-9a-fA-F]\{6\}" pages/ --include="*.wxss" | grep -v "var(--"

# 检查硬编码圆角（应该为0）
grep -rn "border-radius: [0-9]" pages/ --include="*.wxss" | grep -v "var(--"
```

### 2. 共享样式库使用规范 ⭐⭐⭐⭐⭐

`styles/` 目录下有三个共享样式文件，**新页面必须按需 @import，禁止重复定义**：

| 文件 | 职责 | 包含类 |
|------|------|--------|
| `design-tokens.wxss` | CSS 变量定义 | `--color-*`、`--spacing-*`、`--radius-*`、`--font-size-*` |
| `modal-form.wxss` | 弹窗表单 (`mf-*`) | `mf-form`、`mf-row`、`mf-label`、`mf-input` 等 |
| `page-utils.wxss` | 页面通用 UI 块 | 空状态、加载更多、Tag 标签 |

#### `page-utils.wxss` 类速查

**空状态** — 无数据时的占位视图：
```wxss
/* 空状态类已全部移入 app.wxss（全局加载，任何页面无需 @import 即可使用） */
```
```wxml
<view class="empty-state">
  <text class="empty-icon">📦</text>      <!-- 或 <image class="empty-img" src="..."/> -->
  <text class="empty-text">暂无数据</text>
  <text class="empty-hint">试试其他筛选条件</text>
</view>
```

| 类名 | 说明 | 来源 |
|------|------|------|
| `.empty-state` | 容器，flex 列排列，`padding: 60px 20px` | `app.wxss` 全局 |
| `.empty-icon` | 文字 emoji 图标，`48px`，`opacity: 0.4` | `app.wxss` 全局 |
| `.empty-img` | 图片占位，`200rpx` 宽 | `app.wxss` 全局 |
| `.empty-text` | 主说明文字，`font-size-base`，secondary 色 | `app.wxss` 全局 |
| `.empty-hint` | 次说明文字，`font-size-sm`，disabled 色 | `app.wxss` 全局 |

**搜索行** — 页面顶部的搜索框行（也已在 `app.wxss` 全局定义，无需 @import）：

| 类名 | 说明 |
|------|------|
| `.search-row` | 搜索行容器，flex，gap:6px |
| `.search-box` | 搜索框，蓝色半透明背景，round 边框 |
| `.search-icon` | 搜索图标，sm 字号，opacity:0.5 |
| `.search-input` | 搜索输入框 |
| `.search-btn` | 搜索按钮，hover-class="search-btn-hover" |
| `.clear-btn` | 清除按钮，hover-class="clear-btn-hover" |

**加载更多 / 分页**（需要 `@import '/styles/page-utils.wxss';`）：
```wxml
<view class="load-more {{!hasMore ? 'disabled' : ''}}"
      hover-class="load-more-hover"
      bindtap="loadMore">加载更多</view>
<view class="no-more" wx:if="{{!loading && !hasMore}}">没有更多了</view>
<view class="loading-more" wx:if="{{loading}}">加载中...</view>
```

**Tag 标签**：
```wxml
<text class="tag tag-blue">颜色</text>     <!-- 蓝色，等同 tag-color -->
<text class="tag tag-gray">尺码</text>     <!-- 灰色，等同 tag-size -->
<text class="tag tag-green">已完成</text>  <!-- 绿色，等同 tag-success -->
<text class="tag tag-orange">待处理</text> <!-- 橙色，等同 tag-warn -->
<text class="tag tag-red">异常</text>      <!-- 红色，等同 tag-danger -->
<text class="tag tag-muted">已关闭</text>  <!-- 灰色静音 -->
```

> ❌ **禁止**：在页面 wxss 中重新定义 `.empty-state`、`.load-more`、`.no-more`、`.loading-more`、`.tag` 等类，直接引用共享样式即可。  
> ✅ **允许**：用局部选择器覆盖差异（如 `.order-card .empty-state { padding-top: 80rpx; }`）。

---

### 3. 命名规范

**文件命名**:
```
pages/scan/index.js          ✅ 小写 + 连字符
pages/work/components/       ✅ 组件目录
utils/dataValidator.js       ✅ 驼峰命名
```

**变量命名**:
```javascript
// ✅ 推荐
const userInfo = wx.getStorageSync('userInfo');
const scanHandler = new ScanHandler();

// ❌ 避免
const a = wx.getStorageSync('userInfo');
const sh = new ScanHandler();
```

**常量命名**:
```javascript
// ✅ 全大写 + 下划线
const API_BASE_URL = 'https://api.example.com';
const MAX_RETRY_COUNT = 3;

// ❌ 避免
const apiUrl = 'https://api.example.com';
```

### 3. JSDoc 注释规范

**函数注释**:
```javascript
/**
 * 处理扫码结果（必须包含描述）
 * @param {string} codeStr - 扫码内容
 * @param {object} options - 扫码选项
 * @param {string} options.scanType - 扫码类型（auto/cutting/quality/warehouse）
 * @param {number} [options.quantity] - 可选：手动输入数量
 * @returns {Promise<ScanResult>} 扫码处理结果
 * @throws {Error} 当扫码内容无效时抛出错误
 */
async processScanCode(codeStr, options) {
  // ...
}
```

**类型定义** (`types/index.js`):
```javascript
/**
 * @typedef {object} ScanResult
 * @property {boolean} success - 是否成功
 * @property {string} message - 提示消息
 * @property {string} [orderNo] - 订单号（可选）
 * @property {number} [quantity] - 数量（可选）
 */
```

### 4. 错误处理规范

**统一错误处理**:
```javascript
// ✅ 推荐：使用 try-catch + 统一错误提示
try {
  const result = await api.production.executeScan(params);
  wx.showToast({ title: '提交成功', icon: 'success' });
} catch (e) {
  // 使用 errorHandler 统一处理
  const errorMsg = errorHandler.handleError(e, '扫码提交');
  wx.showToast({ title: errorMsg, icon: 'none' });
}

// ❌ 避免：吞掉错误
api.production.executeScan(params).catch(() => {});
```

---

## 🔧 常见问题自查

### 问题1: "扫码领取任务没有反应"

**原因排查**:
```bash
# 1. 检查 WXML 事件绑定
grep "bindtap=\"onReceiveOnly\"" pages/scan/index.wxml

# 2. 检查 JS 方法定义
grep "onReceiveOnly" pages/scan/index.js

# 3. 检查控制台错误
# 打开微信开发者工具 → Console
```

**解决方法**: 
- ✅ 确保 JS 中定义了对应方法
- ✅ 检查方法名拼写是否一致
- ✅ 使用真机调试验证

### 问题2: "数据不刷新"

**原因**:
```javascript
// ❌ 直接修改 data（不会触发视图更新）
this.data.list.push(newItem);

// ✅ 使用 setData
this.setData({
  list: [...this.data.list, newItem]
});
```

### 问题3: "接口调用失败"

**检查清单**:
```javascript
// 1. 检查 Token 是否有效
const token = wx.getStorageSync('token');
console.log('Token:', token);

// 2. 检查网络请求配置（request.js）
// 确保 baseURL 正确
// 确保 timeout 足够长（10s）

// 3. 查看 Network 面板
// 检查请求参数、响应状态码

// 4. 后端日志
// 检查后端是否收到请求
```

### 问题4: "真机扫码无效"

**检查**:
```javascript
// 1. 权限配置（app.json）
{
  "permission": {
    "scope.camera": {
      "desc": "扫码需要使用摄像头"
    }
  }
}

// 2. 扫码逻辑（scan/index.js）
wx.scanCode({
  onlyFromCamera: true,  // 只允许摄像头扫码
  scanType: ['qrCode', 'barCode'],
  success: (res) => {
    console.log('扫码结果:', res.result);
  }
});
```

---

## 📊 代码质量指标

### 当前状态（需定期检查）

```bash
cd miniprogram

# ESLint 检查
npm run 检查 2>&1 | grep "problems"
# 目标: 0 errors, < 10 warnings

# TypeScript 检查
npm run 类型检查 2>&1 | grep "error"
# 目标: 0 errors

# 硬编码检查
grep -rn "border-radius: [0-9]" pages/ --include="*.wxss" | grep -v "var(--" | wc -l
# 目标: 0

# 代码包大小
# 在微信开发者工具中查看
# 目标: 主包 < 2MB
```

### 质量改进计划

**Week 1**:
- [x] 配置 ESLint 强化规则 ✅
- [x] 添加 `lint:fix` 命令 ✅
- [ ] 修复所有 ESLint errors
- [ ] 添加 Prettier

**Week 2**:
- [ ] 为所有关键函数添加 JSDoc
- [ ] 补充 types/index.js 类型定义
- [ ] 使用真机调试测试所有功能

**Week 3**:
- [ ] 性能优化（setData 批量化）
- [ ] 代码复杂度降低（拆分大函数）
- [ ] 添加单元测试

---

## 🚀 快速开始

### 每日开发流程

```bash
# 1. 进入小程序目录
cd miniprogram

# 2. 代码检查
npm run 全面检查

# 3. 修复可自动修复的问题
npm run 修复

# 4. 开发...

# 5. 提交前再次检查
npm run 全面检查

# 6. 真机调试验证
# 打开微信开发者工具 → 真机调试
```

### 关键检查点

- [ ] ESLint 无 errors（warnings < 10）
- [ ] TypeScript 无 errors
- [ ] 所有新增函数有 JSDoc 注释
- [ ] 使用 Design Token（无硬编码）
- [ ] 真机调试通过
- [ ] 性能指标达标（Performance < 2s）

---

**参考文档**:
- [微信官方文档 - 代码质量](https://developers.weixin.qq.com/miniprogram/dev/framework/performance/)
- [ESLint 规则文档](https://eslint.org/docs/latest/rules/)
- [TypeScript JSDoc 参考](https://www.typescriptlang.org/docs/handbook/jsdoc-supported-types.html)

---

*维护者：开发团队*  
*如有问题，查阅 `开发指南.md` 或联系技术负责人*
