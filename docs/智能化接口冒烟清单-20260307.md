# 智能化接口冒烟清单（2026-03-07）

适用范围：本次新增/补齐的智能化相关库表与已接入接口。

目标：确认“库表已存在、接口不报错、前端页面能看到结果、未接线的表不被误判为失败”。

---

## 1. 先看执行结论

本轮云端执行结果可以直接这样判断：

- SQL 2 成功，说明 `t_factory.daily_capacity` 已补上
- SQL 4 报 `Duplicate column name 'daily_capacity'`，这是重复执行后的正常结果，不是失败
- SQL 7 报 1064，不是表有问题，而是把 shell 命令丢进了 SQL 面板
- SQL 5 到 SQL 24 里，除重复执行项外，核心建表均已成功

额外注意：

- 云端 MySQL 当前不支持 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ...`
- 所以后续加列场景，建议先执行 `INFORMATION_SCHEMA.COLUMNS` 查询，再决定是否执行 `ALTER TABLE ... ADD COLUMN ...`

---

## 2. 结构校验清单

在云端数据库面板逐条执行以下校验 SQL。

### 2.1 工厂日产能字段

```sql
SELECT COLUMN_NAME
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_SCHEMA = DATABASE()
  AND TABLE_NAME = 't_factory'
  AND COLUMN_NAME = 'daily_capacity';
```

通过标准：返回 `daily_capacity`

### 2.2 本次智能化核心表

```sql
SHOW TABLES LIKE 't_tenant_smart_feature';
SHOW TABLES LIKE 't_tenant_intelligence_profile';
SHOW TABLES LIKE 't_intelligence_feedback_reason';
SHOW TABLES LIKE 't_intelligence_pain_point';
SHOW TABLES LIKE 't_intelligence_solution_playbook';
SHOW TABLES LIKE 't_intelligence_solution_effect';
SHOW TABLES LIKE 't_factory_skill_matrix';
SHOW TABLES LIKE 't_tenant_business_goal';
SHOW TABLES LIKE 't_mind_push_effect';
SHOW TABLES LIKE 't_worker_skill_growth';
SHOW TABLES LIKE 't_factory_exception_case';
```

通过标准：11 条都能返回表名。

### 2.3 CRM 关联表

```sql
SHOW TABLES LIKE 't_receivable';
SHOW TABLES LIKE 't_customer_portal_token';
```

通过标准：2 条都能返回表名。

---

## 3. 已接业务接口的冒烟清单

下面这些接口在当前仓库里已经存在，可以直接做功能验收。

### 3.1 租户级智能功能开关

后端入口： [backend/src/main/java/com/fashion/supplychain/system/controller/TenantSmartFeatureController.java](backend/src/main/java/com/fashion/supplychain/system/controller/TenantSmartFeatureController.java)

前端落点： [frontend/src/modules/system/pages/System/Profile/components/ProfileInfoTab.tsx](frontend/src/modules/system/pages/System/Profile/components/ProfileInfoTab.tsx)

接口：

- `GET /api/system/tenant-smart-feature/list`
- `POST /api/system/tenant-smart-feature/save`

冒烟步骤：

1. 登录后打开个人中心资料页。
2. 进入智能设置区域，确认能拉到当前开关状态。
3. 任意切换 1 个开关并保存。
4. 刷新页面后再次进入，确认状态保持一致。

通过标准：

- 接口返回 200
- 表 `t_tenant_smart_feature` 中存在当前租户对应记录
- 页面刷新后开关状态不丢失

### 3.2 租户智能画像配置

后端入口： [backend/src/main/java/com/fashion/supplychain/system/controller/TenantIntelligenceProfileController.java](backend/src/main/java/com/fashion/supplychain/system/controller/TenantIntelligenceProfileController.java)

前端落点： [frontend/src/modules/system/pages/System/Profile/components/ProfileInfoTab.tsx](frontend/src/modules/system/pages/System/Profile/components/ProfileInfoTab.tsx)

接口：

- `GET /api/system/tenant-intelligence-profile/current`
- `POST /api/system/tenant-intelligence-profile/save`
- `POST /api/system/tenant-intelligence-profile/reset`

冒烟步骤：

1. 打开个人中心资料页，确认画像配置能正常加载。
2. 修改经营目标、交期预警天数、异常阈值、低毛利阈值并保存。
3. 重新进入页面，确认保存结果回显。
4. 点击重置，确认恢复默认/学习值。

通过标准：

- 三个接口都返回 200
- 表 `t_tenant_intelligence_profile` 有当前租户记录
- 保存和重置后页面显示同步变化

### 3.3 智能反馈原因沉淀

后端入口：

- [backend/src/main/java/com/fashion/supplychain/intelligence/controller/IntelligenceController.java](backend/src/main/java/com/fashion/supplychain/intelligence/controller/IntelligenceController.java)
- [backend/src/main/java/com/fashion/supplychain/intelligence/controller/FeedbackReasonController.java](backend/src/main/java/com/fashion/supplychain/intelligence/controller/FeedbackReasonController.java)

前端落点： [frontend/src/components/common/NodeDetailModal.tsx](frontend/src/components/common/NodeDetailModal.tsx)

接口：

- `POST /api/intelligence/feedback`
- `GET /api/intelligence/feedback-reason/list?limit=20`

冒烟步骤：

1. 打开生产进度节点详情弹窗。
2. 在预计完工时间卡片下方操作反馈条。
3. 选择“可执行”或“不可执行”，填写原因后提交。
4. 再调 `GET /api/intelligence/feedback-reason/list?limit=20` 确认最新记录已出现。

通过标准：

- `POST /api/intelligence/feedback` 返回 200
- `GET /api/intelligence/feedback-reason/list` 返回列表且包含刚提交的记录
- 表 `t_intelligence_feedback_reason` 中能查到新增数据

### 3.4 租户痛点沉淀

后端入口： [backend/src/main/java/com/fashion/supplychain/intelligence/controller/PainPointController.java](backend/src/main/java/com/fashion/supplychain/intelligence/controller/PainPointController.java)

前端落点： [frontend/src/modules/intelligence/pages/IntelligenceCenter/index.tsx](frontend/src/modules/intelligence/pages/IntelligenceCenter/index.tsx)

接口：

- `POST /api/intelligence/pain-point/refresh`
- `GET /api/intelligence/pain-point/list?limit=20`

冒烟步骤：

1. 打开智能运营驾驶舱。
2. 触发一次 pain point refresh。
3. 查看痛点面板是否出现条目。
4. 再次刷新确认数据稳定，不报 500。

通过标准：

- `refresh` 返回 200
- `list` 返回 200 且有数组结构
- 表 `t_intelligence_pain_point` 中有当前租户记录
- 页面面板正常渲染，无空白报错

### 3.5 MindPush 推送状态与规则

后端入口： [backend/src/main/java/com/fashion/supplychain/intelligence/controller/MindPushController.java](backend/src/main/java/com/fashion/supplychain/intelligence/controller/MindPushController.java)

前端服务定义： [frontend/src/services/intelligence/intelligenceApi.ts](frontend/src/services/intelligence/intelligenceApi.ts)

接口：

- `GET /api/intelligence/mind-push/status`
- `POST /api/intelligence/mind-push/rule`
- `POST /api/intelligence/mind-push/check`

冒烟步骤：

1. 先调 `status`，确认规则和最近推送日志能返回。
2. 调 `rule` 保存一个规则变更。
3. 调 `check` 手动触发一次检测。
4. 再调一次 `status`，确认状态接口仍正常。

通过标准：

- 三个接口都返回 200
- 原有 `t_mind_push_log` 查询不报错
- 新表 `t_mind_push_effect` 已存在，但当前版本主要先做结构兜底

### 3.6 客户订单追踪门户

后端入口： [backend/src/main/java/com/fashion/supplychain/production/controller/OrderShareController.java](backend/src/main/java/com/fashion/supplychain/production/controller/OrderShareController.java)

前端服务定义： [frontend/src/services/production/productionApi.ts](frontend/src/services/production/productionApi.ts)

接口：

- `POST /api/production/orders/{id}/share-token`
- `GET /api/public/share/order/{token}`
- `GET /api/public/share/order/{token}/style-cover`

冒烟步骤：

1. 选择一个真实订单，调用分享 token 生成接口。
2. 用返回 token 打开 `/share/{token}` 客户分享页。
3. 确认能取到订单追踪数据、节点进度与款式图。
4. 直接访问公开摘要接口，确认返回结构正确。

通过标准：

- token 生成成功
- 表 `t_customer_portal_token` 出现对应记录
- 公共查询接口可用
- 撤销后 token 失效

---

## 4. 已有智能接口的回归建议

这些不是本次新建表直接新增的接口，但会被这轮智能化链路间接影响，建议最少抽查一遍。

后端入口： [backend/src/main/java/com/fashion/supplychain/intelligence/controller/IntelligenceController.java](backend/src/main/java/com/fashion/supplychain/intelligence/controller/IntelligenceController.java)

建议抽查：

1. `POST /api/intelligence/predict/finish-time`
2. `POST /api/intelligence/delivery-risk/assess`
3. `GET /api/intelligence/style-profile`
4. `GET /api/intelligence/factory-bottleneck`
5. `GET /api/intelligence/supplier-scorecard`
6. `GET /api/intelligence/live-cost?orderId=...`

通过标准：

- 不报 500 / SQL 异常
- 返回结构正常
- 页面无白屏或关键组件报错

---

## 5. 当前只有表、还没有独立业务接口的部分

下面这些表当前已经建好，但仓库里还没有独立 controller/service 使用它们做增删查改，所以验收标准先以“建表成功 + 不影响现有接口”为主，不要误判成“功能没做完就是失败”。

1. `t_intelligence_solution_playbook`
2. `t_intelligence_solution_effect`
3. `t_factory_skill_matrix`
4. `t_tenant_business_goal`
5. `t_mind_push_effect`
6. `t_worker_skill_growth`
7. `t_factory_exception_case`

当前对这 7 张表的验收标准：

1. `SHOW TABLES LIKE ...` 成功
2. 现有 intelligence / system 接口不因新表存在而报错
3. 后续接业务编排时不需要再次补库

---

## 6. 最小数据库抽样校验

如果你要快速确认“接口真的写进库了”，执行以下抽样 SQL 就够了。

```sql
SELECT tenant_id, feature_key, enabled, update_time
FROM t_tenant_smart_feature
ORDER BY update_time DESC
LIMIT 10;

SELECT tenant_id, primary_goal, delivery_warning_days, anomaly_warning_count, low_margin_threshold, update_time
FROM t_tenant_intelligence_profile
ORDER BY update_time DESC
LIMIT 10;

SELECT tenant_id, suggestion_type, accepted, reason_code, operator_name, create_time
FROM t_intelligence_feedback_reason
ORDER BY id DESC
LIMIT 20;

SELECT tenant_id, pain_code, pain_level, trigger_count, current_status, update_time
FROM t_intelligence_pain_point
ORDER BY update_time DESC
LIMIT 20;
```

---

## 7. 发布结论口径

如果你按上面执行完，结论建议统一写成：

- 库表层：通过
- 已开放接口层：通过 / 失败点逐条列出
- 页面层：通过 / 失败点逐条列出
- 未接业务表：结构已就绪，待后续编排接入，不算本次发布阻塞项
