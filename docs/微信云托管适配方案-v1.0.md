# 微信云托管环境 - 智能化改造适配方案

> **版本**：v1.0  
> **日期**：2026-03-29  
> **状态**：规划阶段

---

## 目录

1. [当前部署架构分析](#1-当前部署架构分析)
2. [微信云托管约束与适配](#2-微信云托管约束与适配)
3. [数据库方案调整](#3-数据库方案调整)
4. [改造方案调整](#4-改造方案调整)
5. [实施路线图](#5-实施路线图)

---

## 1. 当前部署架构分析

### 1.1 部署架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       微信云托管（腾讯云开发 CloudBase）                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                     容器服务层                                   │  │
│   │  ┌─────────────────────┐  ┌─────────────────────┐              │  │
│   │  │     frontend        │  │      backend        │              │  │
│   │  │   Nginx + React     │  │   Spring Boot       │              │  │
│   │  │   0.5核 1G          │  │   2核 4G            │              │  │
│   │  │   1-2 实例          │  │   1-4 实例          │              │  │
│   │  │   端口: 80          │  │   端口: 8088        │              │  │
│   │  └─────────────────────┘  └─────────────────────┘              │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                    │                                    │
│   ┌────────────────────────────────┼────────────────────────────────┐  │
│   │                     外部服务层                                   │  │
│   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │  │
│   │  │   MySQL     │  │   Redis     │  │    COS      │             │  │
│   │  │  外部数据库  │  │  腾讯云Redis │  │  对象存储   │             │  │
│   │  │  端口: 3306 │  │  端口: 6379 │  │             │             │  │
│   │  └─────────────┘  └─────────────┘  └─────────────┘             │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│   域名：api.webyszl.cn                                                  │
│   环境 ID：{{ENV_ID}}                                                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 配置文件分析

**cloudbaserc.json 核心配置**：

```json
{
  "envId": "{{ENV_ID}}",
  "framework": {
    "name": "fashion-supplychain",
    "plugins": {
      "backend": {
        "use": "@cloudbase/framework-plugin-container",
        "inputs": {
          "serviceName": "backend",
          "containerPort": 8088,
          "cpu": 2,
          "mem": 4,
          "minNum": 1,
          "maxNum": 4,
          "policyType": "cpu",
          "policyThreshold": 60
        }
      },
      "frontend": {
        "use": "@cloudbase/framework-plugin-container",
        "inputs": {
          "serviceName": "frontend",
          "containerPort": 80,
          "cpu": 0.5,
          "mem": 1,
          "minNum": 1,
          "maxNum": 2
        }
      }
    }
  }
}
```

### 1.3 当前技术栈

| 组件 | 版本 | 部署方式 |
|------|------|---------|
| Spring Boot | 2.7.18 | 容器化 |
| Java | 21 | 容器化 |
| React | 18.2 | Nginx 托管 |
| MySQL | 8.0 | 外部数据库 |
| Redis | - | 腾讯云 Redis |
| COS | - | 腾讯云对象存储 |

---

## 2. 微信云托管约束与适配

### 2.1 平台约束

| 约束项 | 说明 | 影响 |
|--------|------|------|
| **容器化部署** | 只支持 Docker 容器 | 需要调整 Dockerfile |
| **无状态服务** | 容器可能被销毁重建 | 数据必须持久化到外部 |
| **不支持 PostgreSQL** | 微信云托管数据库仅支持 MySQL | 需要外部 PostgreSQL |
| **不支持扩展安装** | 无法在 MySQL 中安装 pgvector | 需要独立的向量数据库 |
| **网络限制** | 容器间通过内网通信 | 需要配置安全组 |

### 2.2 腾讯云支持的服务

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    腾讯云服务矩阵                                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ✅ 可用服务                                                            │
│   ├── TencentDB for MySQL         # 当前使用                            │
│   ├── TencentDB for PostgreSQL    # 可用于替代 MySQL                    │
│   ├── TencentDB for Redis         # 当前使用                            │
│   ├── 腾讯云 ES（Elasticsearch）   # 可用于向量检索                      │
│   ├── 腾讯云向量数据库             # 专用向量存储                        │
│   ├── 腾讯云 COS                  # 当前使用                            │
│   └── 腾讯云 API 网关             # 可选                                │
│                                                                         │
│   ❌ 不可用服务                                                          │
│   ├── 微信云托管内置 PostgreSQL   # 不支持                              │
│   ├── 微信云托管内置向量存储      # 不支持                              │
│   └── 自建数据库                  # 需要外部部署                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.3 推荐架构调整

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    调整后的部署架构                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                   微信云托管（容器服务）                          │  │
│   │  ┌─────────────────────┐  ┌─────────────────────┐              │  │
│   │  │     frontend        │  │      backend        │              │  │
│   │  │   Next.js 15        │  │   Spring Boot 3.2   │              │  │
│   │  │   React 19          │  │   Virtual Threads   │              │  │
│   │  └─────────────────────┘  └─────────────────────┘              │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                    │                                    │
│   ┌────────────────────────────────┼────────────────────────────────┐  │
│   │                     腾讯云服务                                   │  │
│   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │  │
│   │  │ PostgreSQL  │  │   Redis     │  │向量数据库/ES│             │  │
│   │  │ TencentDB   │  │  腾讯云Redis │  │ 腾讯云向量  │             │  │
│   │  │ + pgvector  │  │             │  │             │             │  │
│   │  └─────────────┘  └─────────────┘  └─────────────┘             │  │
│   │  ┌─────────────┐  ┌─────────────┐                              │  │
│   │  │    COS      │  │  LiteLLM    │                              │  │
│   │  │  对象存储   │  │  模型网关   │                              │  │
│   │  └─────────────┘  └─────────────┘                              │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 数据库方案调整

### 3.1 方案对比

| 方案 | 优势 | 劣势 | 推荐度 |
|------|------|------|--------|
| **方案A：腾讯云 PostgreSQL** | 原生支持 pgvector、功能完整 | 需要迁移数据、成本增加 | ⭐⭐⭐⭐⭐ |
| **方案B：继续 MySQL + 腾讯云向量数据库** | 改动最小、风险低 | 架构复杂、数据一致性难保证 | ⭐⭐⭐⭐ |
| **方案C：继续 MySQL + 腾讯云 ES** | ES 功能强大、支持向量检索 | 成本高、维护复杂 | ⭐⭐⭐ |
| **方案D：继续 MySQL + Qdrant 云服务** | 当前已使用 Qdrant | 外部服务、网络延迟 | ⭐⭐ |

### 3.2 推荐方案：腾讯云 PostgreSQL + pgvector

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    数据库架构调整                                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   当前架构：                                                             │
│   ┌─────────────┐                                                       │
│   │   MySQL     │ ← 业务数据                                            │
│   │  TencentDB  │                                                       │
│   └─────────────┘                                                       │
│                                                                         │
│   目标架构：                                                             │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                    PostgreSQL (TencentDB)                        │  │
│   │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │  │
│   │  │    业务数据      │  │    向量数据     │  │    时序数据     │ │  │
│   │  │  t_production_* │  │ t_intelligence_ │  │   t_metrics     │ │  │
│   │  │  t_finance_*    │  │    memory       │  │  (TimescaleDB)  │ │  │
│   │  │  t_warehouse_*  │  │  (pgvector)     │  │                 │ │  │
│   │  └─────────────────┘  └─────────────────┘  └─────────────────┘ │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.3 腾讯云 PostgreSQL 配置

```yaml
# 腾讯云 PostgreSQL 配置建议
TencentDB for PostgreSQL:
  版本: PostgreSQL 16
  规格: 
    - 开发环境: 2核4G
    - 生产环境: 4核8G
  存储: 
    - 开发环境: 50GB SSD
    - 生产环境: 200GB SSD
  扩展:
    - pgvector    # 向量存储
    - pg_trgm     # 文本搜索
    - btree_gin   # 索引优化
  
  费用估算:
    - 开发环境: 约 ¥300/月
    - 生产环境: 约 ¥800/月
```

### 3.4 数据迁移策略

```bash
# 迁移步骤

# 1. 创建腾讯云 PostgreSQL 实例
# 在腾讯云控制台创建 PostgreSQL 16 实例

# 2. 配置 pgvector 扩展
psql -h <pg-host> -U <user> -d fashion_supplychain
CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS pg_trgm;

# 3. 使用 pgloader 迁移 MySQL 数据
# 安装 pgloader
brew install pgloader  # macOS
# 或 apt-get install pgloader  # Linux

# 4. 创建迁移配置文件
cat > migration.load << 'EOF'
LOAD DATABASE
    FROM mysql://root:changeme@<mysql-host>:3306/fashion_supplychain
    INTO postgresql://<user>:<password>@<pg-host>:5432/fashion_supplychain

WITH include drop, create tables, create indexes, reset sequences,
     workers = 4, concurrency = 2

ALTER SCHEMA 'fashion_supplychain' RENAME TO 'public'
EOF

# 5. 执行迁移
pgloader migration.load

# 6. 验证数据
psql -h <pg-host> -U <user> -d fashion_supplychain -c "SELECT COUNT(*) FROM t_production_order;"
```

---

## 4. 改造方案调整

### 4.1 后端升级方案（适配微信云托管）

#### 4.1.1 Dockerfile 调整

```dockerfile
# backend/Dockerfile - 升级到 Spring Boot 3.2

# ================================
# 阶段一：构建
# ================================
FROM maven:3.9.9-eclipse-temurin-21 AS build
WORKDIR /app

# 复制 pom.xml 并下载依赖
COPY pom.xml ./
RUN mvn -q -e -DskipTests dependency:go-offline

# 复制源码并构建
COPY src ./src
RUN mvn -e -DskipTests package

# ================================
# 阶段二：运行
# ================================
FROM eclipse-temurin:21-jre
WORKDIR /app

# 安装必要工具
RUN apt-get update \
  && apt-get install -y --no-install-recommends ca-certificates curl \
  && rm -rf /var/lib/apt/lists/*

# 设置时区
ENV TZ=Asia/Shanghai
RUN ln -snf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime

# 创建上传目录
RUN mkdir -p /uploads/tenants

# 复制构建产物
COPY --from=build /app/target/*.jar ./app.jar

# 暴露端口
EXPOSE 8088

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
  CMD curl -f http://localhost:8088/actuator/health || exit 1

# 环境变量
ENV SPRING_PROFILES_ACTIVE=prod

# JVM 启动参数（启用 Virtual Threads）
# Spring Boot 3.2 + Java 21 支持 Virtual Threads
CMD ["java", \
  "-XX:TieredStopAtLevel=1", \
  "-Dspring.jmx.enabled=false", \
  "-Duser.timezone=Asia/Shanghai", \
  "-Djavax.net.ssl.trustStore=/etc/ssl/certs/java/cacerts", \
  "--enable-preview", \
  "-jar", "/app/app.jar"]
```

#### 4.1.2 cloudbaserc.json 调整

```json
{
  "envId": "{{ENV_ID}}",
  "framework": {
    "name": "fashion-supplychain",
    "plugins": {
      "backend": {
        "use": "@cloudbase/framework-plugin-container",
        "inputs": {
          "serviceName": "backend",
          "servicePath": "/backend",
          "localPath": "./backend",
          "containerPort": 8088,
          "cpu": 2,
          "mem": 4,
          "minNum": 1,
          "maxNum": 4,
          "policyType": "cpu",
          "policyThreshold": 60,
          "envVariables": {
            "PORT": "8088",
            "SPRING_DATASOURCE_URL": "jdbc:postgresql://{{PG_HOST}}:5432/fashion_supplychain?useUnicode=true&characterEncoding=UTF-8",
            "SPRING_DATASOURCE_USERNAME": "{{PG_USER}}",
            "SPRING_DATASOURCE_PASSWORD": "{{PG_PASSWORD}}",
            "SPRING_DATASOURCE_DRIVER_CLASS_NAME": "org.postgresql.Driver",
            "SPRING_REDIS_HOST": "{{REDIS_HOST}}",
            "SPRING_REDIS_PORT": "6379",
            "APP_AUTH_JWT_SECRET": "{{JWT_SECRET}}",
            "WECHAT_MP_APPID": "{{WECHAT_APPID}}",
            "WECHAT_MP_SECRET": "{{WECHAT_SECRET}}",
            "SPRING_PROFILES_ACTIVE": "prod",
            "AI_EMBEDDING_API_KEY": "{{OPENAI_API_KEY}}",
            "AI_EMBEDDING_BASE_URL": "https://api.openai.com/v1"
          },
          "customLogs": "stdout",
          "healthPath": "/actuator/health/liveness"
        }
      },
      "frontend": {
        "use": "@cloudbase/framework-plugin-container",
        "inputs": {
          "serviceName": "frontend",
          "servicePath": "/",
          "localPath": "./frontend",
          "containerPort": 80,
          "cpu": 0.5,
          "mem": 1,
          "minNum": 1,
          "maxNum": 2,
          "envVariables": {
            "BACKEND_URL": "http://backend:8088"
          }
        }
      }
    }
  }
}
```

### 4.2 前端升级方案（适配微信云托管）

#### 4.2.1 Next.js Dockerfile

```dockerfile
# frontend/Dockerfile - Next.js 15

# ================================
# 阶段一：构建
# ================================
FROM node:20-alpine AS builder

WORKDIR /app

# 安装依赖
COPY package.json package-lock.json* ./
RUN npm ci --legacy-peer-deps

# 复制源码
COPY . .

# 构建参数
ARG BUILD_DATE=unknown
ARG NEXT_PUBLIC_API_URL=https://api.webyszl.cn

# 构建
ENV NEXT_TELEMETRY_DISABLED=1
RUN npm run build

# ================================
# 阶段二：运行
# ================================
FROM node:20-alpine AS runner

WORKDIR /app

# 设置环境
ENV NODE_ENV=production
ENV NEXT_TELEMETRY_DISABLED=1

# 创建非 root 用户
RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

# 复制构建产物
COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

# 切换用户
USER nextjs

# 暴露端口
EXPOSE 80

# 环境变量
ENV PORT=80
ENV HOSTNAME="0.0.0.0"

# 启动
CMD ["node", "server.js"]
```

#### 4.2.2 next.config.js

```javascript
// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',  // 微信云托管需要 standalone 输出
  reactStrictMode: true,
  poweredByHeader: false,
  
  // 环境变量
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL || 'https://api.webyszl.cn',
  },
  
  // API 代理（开发环境）
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: `${process.env.BACKEND_URL || 'http://backend:8088'}/api/:path*`,
      },
    ];
  },
  
  // 图片优化
  images: {
    domains: ['*.webyszl.cn', '*.myqcloud.com'],
    unoptimized: process.env.NODE_ENV === 'production',
  },
};

module.exports = nextConfig;
```

### 4.3 数据库连接调整

#### 4.3.1 application-prod.yml

```yaml
# application-prod.yml - PostgreSQL 配置

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # JPA 配置
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        default_schema: public

  # Flyway 配置
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration/postgresql
    validate-on-migrate: true

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    default-schema: public
  global-config:
    db-config:
      schema: public
```

#### 4.3.2 pom.xml 依赖调整

```xml
<!-- PostgreSQL 驱动 -->
<dependency>
    <groupId>org.postgresql</groupId>
    <artifactId>postgresql</artifactId>
    <version>42.7.2</version>
</dependency>

<!-- 移除 MySQL 驱动（或保留兼容） -->
<!-- <dependency> -->
<!--     <groupId>mysql</groupId> -->
<!--     <artifactId>mysql-connector-java</artifactId> -->
<!-- </dependency> -->
```

---

## 5. 实施路线图

### 5.1 阶段划分

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    微信云托管改造路线图                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   Phase 0：基础设施准备（1周）                                           │
│   ├── 创建腾讯云 PostgreSQL 实例                                        │
│   ├── 配置 pgvector 扩展                                                │
│   ├── 配置网络和安全组                                                  │
│   └── 数据迁移测试                                                      │
│                                                                         │
│   Phase 1：数据库迁移（2周）                                             │
│   ├── 编写迁移脚本                                                      │
│   ├── 数据迁移执行                                                      │
│   ├── 应用适配                                                          │
│   └── 验证测试                                                          │
│                                                                         │
│   Phase 2：后端升级（2周）                                               │
│   ├── Spring Boot 3.2 升级                                             │
│   ├── PostgreSQL 适配                                                   │
│   ├── 智能化功能开发                                                    │
│   └── 部署测试                                                          │
│                                                                         │
│   Phase 3：前端升级（2周）                                               │
│   ├── Next.js 15 搭建                                                   │
│   ├── 页面迁移                                                          │
│   ├── 智能组件开发                                                      │
│   └── 部署测试                                                          │
│                                                                         │
│   Phase 4：智能化功能（4周）                                             │
│   ├── 感知层开发                                                        │
│   ├── 判断层开发                                                        │
│   ├── 行动层开发                                                        │
│   ├── 学习层开发                                                        │
│   └── 集成测试                                                          │
│                                                                         │
│   Phase 5：上线切换（1周）                                               │
│   ├── 灰度发布                                                          │
│   ├── 监控配置                                                          │
│   ├── 全量切换                                                          │
│   └── 稳定性观察                                                        │
│                                                                         │
│   总计：约 12 周                                                         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.2 详细任务清单

#### Phase 0：基础设施准备

| 任务ID | 任务名称 | 负责人 | 工作量 | 交付物 |
|--------|---------|--------|--------|--------|
| P0-001 | 创建腾讯云 PostgreSQL 实例 | 运维 | 0.5天 | 实例创建完成 |
| P0-002 | 配置 pgvector 扩展 | 运维 | 0.5天 | 扩展安装成功 |
| P0-003 | 配置网络和安全组 | 运维 | 0.5天 | 网络连通 |
| P0-004 | 创建测试数据库 | 运维 | 0.5天 | 测试库就绪 |
| P0-005 | 编写迁移测试脚本 | 后端 | 1天 | 测试脚本 |
| P0-006 | 执行迁移测试 | 后端 | 1天 | 测试报告 |

#### Phase 1：数据库迁移

| 任务ID | 任务名称 | 负责人 | 工作量 | 交付物 |
|--------|---------|--------|--------|--------|
| P1-001 | 编写完整迁移脚本 | 后端 | 2天 | 迁移脚本 |
| P1-002 | 数据迁移执行 | 后端 | 1天 | 迁移完成 |
| P1-003 | 数据验证 | 后端 | 1天 | 验证报告 |
| P1-004 | 应用代码适配 | 后端 | 3天 | 适配代码 |
| P1-005 | 功能测试 | 测试 | 2天 | 测试报告 |

#### Phase 2：后端升级

| 任务ID | 任务名称 | 负责人 | 工作量 | 交付物 |
|--------|---------|--------|--------|--------|
| P2-001 | Spring Boot 3.2 升级 | 后端 | 3天 | 升级代码 |
| P2-002 | Jakarta EE 迁移 | 后端 | 2天 | 迁移代码 |
| P2-003 | Virtual Threads 配置 | 后端 | 1天 | 配置文件 |
| P2-004 | Dockerfile 更新 | 后端 | 1天 | Dockerfile |
| P2-005 | 部署测试 | 后端 | 1天 | 测试报告 |

#### Phase 3：前端升级

| 任务ID | 任务名称 | 负责人 | 工作量 | 交付物 |
|--------|---------|--------|--------|--------|
| P3-001 | Next.js 15 项目搭建 | 前端 | 2天 | 项目骨架 |
| P3-002 | 核心页面迁移 | 前端 | 4天 | 页面代码 |
| P3-003 | 智能组件开发 | 前端 | 2天 | 组件代码 |
| P3-004 | Dockerfile 更新 | 前端 | 1天 | Dockerfile |
| P3-005 | 部署测试 | 前端 | 1天 | 测试报告 |

### 5.3 风险与对策

| 风险 | 影响 | 概率 | 对策 |
|------|------|------|------|
| 数据库迁移失败 | 高 | 中 | 完整备份 + 增量同步 + 回滚脚本 |
| Spring Boot 升级失败 | 高 | 低 | 分模块升级 + 充分测试 |
| 微信云托管兼容问题 | 中 | 低 | 提前测试 + 联系腾讯云支持 |
| 性能下降 | 中 | 中 | 压力测试 + 性能优化 |

---

## 附录

### A. 腾讯云服务费用估算

| 服务 | 规格 | 月费用 | 说明 |
|------|------|--------|------|
| 微信云托管 - 后端 | 2核4G | ¥300 | 1实例 |
| 微信云托管 - 前端 | 0.5核1G | ¥100 | 1实例 |
| TencentDB PostgreSQL | 2核4G | ¥300 | 开发环境 |
| TencentDB PostgreSQL | 4核8G | ¥800 | 生产环境 |
| TencentDB Redis | 1G | ¥100 | 当前使用 |
| **总计（开发）** | - | **¥800/月** | - |
| **总计（生产）** | - | **¥1300/月** | - |

### B. 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0 | 2026-03-29 | 初始版本 |
