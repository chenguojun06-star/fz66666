# 2026-09-13 全天核实与收尾清单

> 核实时间：2026-09-13 22:30
> 核实方式：git log / git status / git fetch 比对 / CI 列表 / 配置与代码逐项 grep
> 结论口径：**代码层面已核实的事实** 与 **需要人去线上确认的事项** 严格分开标注

---

## 一、今天做了什么（已核实的硬数据）

| 指标 | 数值 |
|---|---|
| 提交数 | **50**（00:05 ~ 21:48） |
| 改动文件 | 86 个（去重） |
| 代码量 | **+6338 / -679** |
| CI | 29 次：27 success、2 cancelled，**0 失败** |
| 本地 vs 远程 | 完全同步（领先 0 / 落后 0） |
| HEAD | `0fc8178fc` 21:48 D-397 小云对话气泡时间戳 |
| 工作区 | 干净，仅 2 个未跟踪文件（见 P0-4） |

提交分布：D-385 ×14、D-386 ×6、D-388 ×2、D-379 ×2，D-387/389/390/391/392/393/394/395/396/397 各 1。

### 四条主线

**1. AI 向量底座（D-385/386）— 已闭环**
Embedding 提供方踩坑链：DeepSeek 无接口(404) → Voyage 封大陆 IP → 智谱拦机房 IP → **硅基流动 bge-m3 定版(1024 维)**。
真凶最后查明是 **鉴权头写死 deepseekApiKey**（`28a13c043`），前面所有"地理封锁"结论作废。
配套：Qdrant 点 ID 转 UUID、建集合 POST→PUT、401 入熔断、以图搜款升级向量优先两级策略、启动补齐存量向量（断点续跑）。

**2. 检索质量升级（D-387/389 + rerank + P0-3 + sparse）— 代码就位，默认全关**
- Rerank 精排层（硅基流动 bge-reranker-v2-m3）+ 三层降级
- `recall-top-k` 3→20（补齐三处配置，并接进此前硬编码的 AgenticRagService）
- sparse 写入首次实现 + Query API 响应解析修正 + RRF 版本兼容
- sparse 重灌端点（断点续跑、默认关闭）
- 向量写入批量化（120 张表 240 次请求 → 6 次）

**3. 生产事故修复 — 根因已修并推送**
| 时间 | 事故 | 根因 |
|---|---|---|
| 12:16 起 | backend-2577/78/79 连续部署失败 | D-388 去掉 `-XX:TieredStopAtLevel=1`，1 vCPU 下 C2 抢 CPU，冷启动 >118s（`61dcfdcde` 已回滚） |
| 14:30 | 连接池耗尽刷屏 | `SchemaVectorManager.init()` **每次启动无条件全量重灌**（`6fabfd2c3` 加 Redis 幂等标记） |
| 17:40 | 小云完全答不出 | Qdrant match 过滤器批量修 9 处**漏了 search() 主路径**（`8aae07259`） |
| 14:35 | 检索全废 | 11 处 `putObject("match").put("integer")` 非法写法（`76b9279ef`） |
| 13:51 | 只剩看板没有文字 | 卡片与文字两条链路用了不同字符串（`acc66a97f` + `bd0228a3d`） |

**4. 多 Agent 与配额（D-391~397）— 已上线，但属降级收尾**
- D-391 配额用完透传友好文案 + 查看/重置接口
- D-392/395 日配额 20 万 → 50 万 → **200 万**
- D-393 图路径补发 answer 事件、D-394 过滤"分析中"占位话术
- **D-396 多 Agent 图流式路由默认关闭**（实测单次 5-8 次 LLM、20-60s、专家结果常为空）
- D-397 气泡时间戳

---

## 二、仍开放的未完成项（逐条核实过）

### P0 — 影响线上，需要人操作

| # | 事项 | 当前核实状态 | 要做 |
|---|---|---|---|
| 1 | **混合检索仍未激活** | `application.yml:474` `named-vectors: ${QDRANT_NAMED_VECTORS:false}` — 确认仍为 false | 设新集合名 + `named-vectors=true` → 重启 → `POST /api/intelligence/qdrant/backfill-sparse-vectors` → 查进度 |
| 2 | **存活探针初始延迟** | `61dcfdcde` 只在 `cloudbaserc.json` 补了 `healthPath`；但部署走控制台配置，**cloudbaserc 不一定被读取** | 云托管控制台 → backend 服务设置 → 存活探针 initialDelay ≥300s（或改 startupProbe） |
| 3 | **50 个提交全部缺线上回归** | 多数只做了 `mvn compile`；今日 29 次部署，无人做真机验证 | 按 `docs/小云AI验证清单与优化路线-2026-09-13.md` 跑第 1 步（5 分钟 10 条） |
| 4 | **`check-cloud-health.sh` 已过时** | 脚本指向 `106.53.5.62`；已确认**当前环境是微信云托管**，该机是历史遗留 | 删掉，或改写成云托管版；**不要提交进仓库**（会误导后人） |

### P1 — 已知缺口

5. **样衣多色多码 6 项未真机回归**：多色领取 / 3-3 进度 / PC 批量完成四处 / 填数量完成 / 两端阶段数一致 / 指派多人额度独立。
6. **D-386 扫码页状态口径未统一**：扫码走 `process-config status`，详情与列表走件数聚合，统一需改后端状态推导（影响 PC，待评估）。
7. **日配额 200 万是拍脑袋值**：D-395 提额的理由（多 Agent 图烧配额）在 D-396 关掉图路由后**已不成立**。建议观察 2-3 天实际用量后回调，否则等于取消了成本控制闸门。
8. **评测集 A 组基线未跑**：`docs/小云AI评测集-2026-09-13.md` 30 条已写好，没有跑过一次。没有基线，混合检索开了也无法证明变好还是变坏。
9. **`upsertArchivalTiered` / `upsertStyleImageVector` 仍只写 dense**（按范围只改了 `upsertVector`），归档与款式图片向量未享受 sparse。
10. **IDF 未做**：`sparse-v2` 只做了去中文单字符噪声 + 哈希 10 万→31 位，仍只有 TF 无 IDF，需 df 统计基础设施 + 两阶段重建。

### P2 — 长期遗留（今天没动）

46 个历史横线编码未归一化 · D-256 生产库回填 SQL 未执行 · `merchandiser/xxxAssigneeId` 需补落库 · D-381 批量内部仍逐条 `submitScan` · D-379 未屏蔽"他人已领颜色"。

---

## 三、可升级的点（按性价比排序，不要一次全做）

1. **先停手观察（最高性价比）**
   今天 29 次部署 ≈ 29 次冷启动（每次 ~2 分钟）+ 每次重启都可能触发启动期重活。**今天已明确建议不再推代码**。
   明天集中做一次验证，比继续堆改动价值高。

2. **跑评测集建基线**（1 小时，解锁后面所有判断）
   没有基线 → 开 rerank、开混合检索都只能凭感觉。这是"数据驱动"的前提。

3. **再动混合检索**（P0-1 的 4 步）
   注意顺序：**先跑基线 → 再开混合检索 → 再跑基线对比**。

4. **配额回调**
   观察 2-3 天，把 `ai.budget.tenant-daily-token-limit` 从 200 万调到实际 P95 用量的 1.5 倍左右。

5. **冷启动优化**（1 vCPU 是硬约束）
   可选方向：启动期初始化异步化（COS / ProcessParentMappingService）、AppCDS。
   ⚠️ 不要再动 JIT 分层编译 —— 今天已因此搞挂 3 次部署。

6. **清理过时资产**
   `check-cloud-health.sh`（错环境）、根目录一批面向本地 `fashion-mysql-simple` 的 `*.sh` 都没标注适用范围，容易误用。

---

## 四、今天买来的三条纪律（明天继续遵守）

1. **推送前本地 `mvn compile` = 硬性动作**（今天因没验证搞挂 3 次部署，后来 pre-push hook 已内置 `mvn compile`）。
2. **批量修复必须全仓 grep 收尾**：同一模式改 9 处漏 1 处，而漏的那处恰是主检索路径 → 整条链路全废。
3. **"这个功能到底有没有在跑"必须逐条核实，不能只看类是否存在**：今天踩了三次（CohereRerank 关着、hybridSearch 恒返回空、SemanticCache 早就有了）。
4. **部署频繁时，任何"启动即全量重活"的代码都是生产事故**：今天同类反模式出现三次（StyleVectorBackfill / SchemaVectorManager.preheat / 排查中）。

---

## 五、需要你确认的 3 件事

1. **今晚是否收工？** 我建议是。明天先跑验证清单，再决定下一步。
2. **混合检索激活要不要排期？** 需要一次重启 + 一次重灌（≈100~300 次 embedding 调用），需要一个窗口期。
3. **两个未跟踪文件怎么处理？**
   - `docs/小云AI验证清单与优化路线-2026-09-13.md` → 建议提交
   - `check-cloud-health.sh` → 建议删除（指向已废弃的 106.53.5.62）

---

## 附：无法由代码核实、必须去云托管控制台看的事项

- 控制台：https://cloud.weixin.qq.com/cloudrun/service/backend （环境 prod-7g0s09nb52f17608）
- 日志页搜 `Connection is not available` → 连接池是否彻底恢复
- 日志页搜 `schema-vector-preheat` → 确认预热只跑过一次
- 部署发布页 → 确认最后一次部署（D-397）是否成功、实例是否 Ready
