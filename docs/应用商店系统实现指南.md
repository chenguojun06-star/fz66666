# 📱 SaaS 应用商店系统 - 完整实现指南

> **实现日期**：2026-02-10  
> **功能概述**：应用商店 + 订阅购买 + 支付流程  
> **状态**：✅ 数据库已建表，✅ 后端代码已完成，✅ 前端页面已完成

---

## 🎯 功能总览

### 核心功能流程
```
[应用商店首页] 
    ↓ 点击应用图标
[应用详情弹窗] 
    ↓ 点击购买按钮
[填写购买信息] 
    ↓ 提交订单
[支付页面] 
    ↓ 支付成功
[开通应用订阅]
```

### 已完成的模块
- ✅ **数据库表结构**（4张表）
  - `t_app_store` - 应用商店（可购买的应用列表）
  - `t_tenant_subscription` - 租户订阅（已购买的应用）
  - `t_app_order` - 应用订单（购买订单记录）
  - `t_app_payment` - 支付记录（支付流水）

- ✅ **后端 API**
  - `POST /api/system/app-store/list` - 获取应用列表
  - `GET /api/system/app-store/{id}` - 获取应用详情
  - `POST /api/system/app-store/create-order` - 创建订单
  - `POST /api/system/app-store/my-subscriptions` - 我的订阅

- ✅ **前端页面**
  - 应用商店首页 - 卡片网格布局 + Badge 标签（热门/新应用）
  - 应用详情弹窗 - 功能列表 + 价格方案
  - 购买订单弹窗 - 表单填写 + 发票信息

---

## 🗃️ 数据库表结构

### 1. t_app_store（应用商店）
存储可购买的应用，已初始化 4 个应用：

| app_code | app_name | price_monthly | is_hot | 功能 |
|----------|----------|---------------|--------|------|
| ORDER_SYNC | 下单对接 | ¥699/月 | 🔥 热门 | 订单同步、自动下单、进度跟踪、异常预警 |
| QUALITY_FEEDBACK | 质检反馈 | ¥499/月 | - | 质检报告、实时反馈、不良品追踪、数据统计 |
| LOGISTICS_SYNC | 物流对接 | ¥599/月 | 🔥 热门 | 物流跟踪、自动签收、运费计算、轨迹查询 |
| PAYMENT_SYNC | 付款对接 | ¥799/月 | - | 对账单同步、自动核销、付款申请、财务报表 |

**价格方案**：
- 月付：按月订阅
- 年付：按年订阅（通常有优惠）
- 买断：一次性购买永久使用
- 试用：免费试用 7 天

---

## 🎨 前端页面展示效果

### 应用商店首页（卡片布局）
```
┌──────────┬──────────┬──────────┬──────────┐
│  [热门]  │          │  [热门]  │          │
│    📋    │    ✅    │    🚚    │    💳    │
│  下单对接 │  质检反馈 │  物流对接 │  付款对接 │
│  ¥699/月 │  ¥499/月 │  ¥599/月 │  ¥799/月 │
│ 供应链对接│ 供应链对接│ 供应链对接│ 供应链对接│
└──────────┴──────────┴──────────┴──────────┘
```

**设计特点**：
- ✅ 卡片悬浮动画（hover 上移 8px）
- ✅ 渐变背景图标区域
- ✅ Badge 标签（红色=热门，蓝色=新应用）
- ✅ 响应式布局（xs/sm/md/lg/xl）

---

## 🛠️ 使用方法

### 1. 访问应用商店
在系统菜单中添加应用商店入口（需要配置路由）：

```typescript
// frontend/src/routeConfig.ts
{
  path: '/system/app-store',
  component: () => import('@/modules/system/pages/AppStore'),
  meta: { title: '应用商店', requiresAuth: true }
}
```

### 2. 浏览应用
```
1. 进入应用商店页面
2. 浏览所有可购买的应用卡片
3. 查看应用图标、名称、价格、分类
4. 点击卡片查看详情
```

### 3. 查看应用详情
点击应用卡片后弹出详情窗口，显示：
- 应用简介
- 核心功能列表（带绿色✓图标）
- 价格方案（月付/年付/买断）
- 试用按钮（如果支持试用）

### 4. 购买应用
点击"立即购买"按钮，弹出订单表单：
```
字段：
- 订阅类型（月付/年付/买断/试用）
- 用户数量（1-999）
- 联系人姓名*
- 联系电话*（手机号验证）
- 联系邮箱（邮箱验证）
- 公司名称
- 是否需要发票
  - 发票抬头*
  - 纳税人识别号*
```

### 5. 提交订单
提交后创建订单记录，订单状态为 `PENDING`（待支付）

---

## 🔧 开发扩展

### 待实现功能（TODO）

#### 1. 支付集成
```java
// 需要实现支付模块
@PostMapping("/api/system/payment/create")
public Result<PaymentInfo> createPayment(@RequestBody PaymentRequest request) {
    // 1. 查询订单
    AppOrder order = appOrderService.getByOrderNo(request.getOrderNo());
    
    // 2. 调用微信/支付宝支付SDK
    String payUrl = wechatPayService.createNativePay(order);
    
    // 3. 返回支付二维码
    return Result.success(new PaymentInfo(payUrl));
}
```

#### 2. 订单支付成功回调
```java
@PostMapping("/api/system/payment/callback")
public String paymentCallback(@RequestBody Map<String, Object> data) {
    // 1. 验签
    boolean valid = wechatPayService.verifySignature(data);
    
    // 2. 更新订单状态为 PAID
    AppOrder order = appOrderService.getByOrderNo(orderNo);
    order.setStatus("PAID");
    order.setPaymentTime(LocalDateTime.now());
    appOrderService.updateById(order);
    
    // 3. 创建订阅记录
    createSubscription(order);
    
    return "SUCCESS";
}
```

#### 3. 自动创建订阅
```java
private void createSubscription(AppOrder order) {
    TenantSubscription subscription = new TenantSubscription();
    subscription.setSubscriptionNo(tenantSubscriptionService.generateSubscriptionNo());
    subscription.setTenantId(order.getTenantId());
    subscription.setAppId(order.getAppId());
    subscription.setAppCode(order.getAppCode());
    subscription.setSubscriptionType(order.getSubscriptionType());
    subscription.setStartTime(LocalDateTime.now());
    
    // 计算到期时间
    if ("MONTHLY".equals(order.getSubscriptionType())) {
        subscription.setEndTime(LocalDateTime.now().plusMonths(1));
    } else if ("YEARLY".equals(order.getSubscriptionType())) {
        subscription.setEndTime(LocalDateTime.now().plusYears(1));
    } else if ("PERPETUAL".equals(order.getSubscriptionType())) {
        subscription.setEndTime(null); // 永久
    }
    
    subscription.setStatus("ACTIVE");
    tenantSubscriptionService.save(subscription);
}
```

#### 4. 权限控制（基于订阅）
```java
// 在登录时加载用户已订阅的应用
@Service
public class TenantPermissionService {
    
    public List<String> getUserSubscribedApps(Long tenantId) {
        QueryWrapper<TenantSubscription> wrapper = new QueryWrapper<>();
        wrapper.eq("tenant_id", tenantId);
        wrapper.eq("status", "ACTIVE");
        wrapper.gt("end_time", LocalDateTime.now()); // 未过期
        
        List<TenantSubscription> subscriptions = tenantSubscriptionService.list(wrapper);
        return subscriptions.stream()
            .map(TenantSubscription::getAppCode)
            .collect(Collectors.toList());
    }
    
    public boolean hasAppPermission(Long tenantId, String appCode) {
        List<String> apps = getUserSubscribedApps(tenantId);
        return apps.contains(appCode);
    }
}
```

#### 5. 菜单动态显示（根据订阅）
```java
// 前端在路由守卫中检查应用权限
router.beforeEach((to, from, next) => {
  const requiredApp = to.meta.requiredApp; // 如 'PRODUCTION'
  if (requiredApp) {
    const subscribedApps = store.state.user.subscribedApps;
    if (!subscribedApps.includes(requiredApp)) {
      message.error('您未订阅此应用，请前往应用商店购买');
      next('/system/app-store');
      return;
    }
  }
  next();
});
```

---

## 📝 权限配置

### 后端权限（需要在数据库添加）
```sql
-- 应用商店相关权限
INSERT INTO t_permission (permission_code, permission_name, permission_type) VALUES
('MENU_APP_STORE_VIEW', '查看应用商店', 'MENU'),
('MENU_APP_STORE_BUY', '购买应用', 'ACTION'),
('MENU_APP_SUBSCRIPTION_VIEW', '查看我的订阅', 'MENU'),
('MENU_APP_SUBSCRIPTION_MANAGE', '管理订阅', 'ACTION');

-- 分配给管理员角色
INSERT INTO t_role_permission (role_id, permission_id)
SELECT 1, id FROM t_permission WHERE permission_code LIKE 'MENU_APP_%';
```

---

## 🎉 功能演示

### 操作步骤
```bash
# 1. 启动系统
cd /Users/guojunmini4/Documents/服装66666
./dev-public.sh

# 2. 编译后端（如果代码有修改）
cd backend
mvn clean compile

# 3. 访问应用商店（需要先配置路由）
浏览器打开：http://localhost:5173/system/app-store
```

### 测试数据库
```bash
# 查看应用列表
docker exec fashion-mysql-simple mysql -uroot -pchangeme fashion_supplychain \
  -e "SELECT * FROM t_app_store WHERE status='PUBLISHED' ORDER BY sort_order;"

# 查看订单记录
docker exec fashion-mysql-simple mysql -uroot -pchangeme fashion_supplychain \
  -e "SELECT order_no, app_name, total_amount, status FROM t_app_order;"

# 查看订阅记录
docker exec fashion-mysql-simple mysql -uroot -pchangeme fashion_supplychain \
  -e "SELECT subscription_no, app_name, status, start_time, end_time FROM t_tenant_subscription;"
```

---

## 🚀 下一步计划

### 短期（1周内）
- [ ] 配置前端路由（将应用商店加入菜单）
- [ ] 实现支付模块（微信支付/支付宝）
- [ ] 订单支付成功后自动创建订阅
- [ ] 我的订阅页面（查看已购买应用）

### 中期（1个月内）
- [ ] 权限控制（根据订阅动态显示菜单）
- [ ] 订阅续费功能
- [ ] 订阅升级/降级
- [ ] 发票管理

### 长期（3个月内）
- [ ] 优惠券/促销活动
- [ ] 推荐奖励机制
- [ ] 应用使用统计
- [ ] 自动扣费（银行卡绑定）

---

## 📚 相关文件位置

### 数据库SQL
- `/backend/sql/V20260210__app_store.sql` - 应用商店建表SQL

### 后端代码
- `/backend/src/main/java/com/fashion/supplychain/system/`
  - `entity/AppStore.java` - 应用实体
  - `entity/TenantSubscription.java` - 订阅实体
  - `entity/AppOrder.java` - 订单实体
  - `mapper/AppStoreMapper.java` - 数据访问层
  - `service/AppStoreService.java` - 业务逻辑层
  - `controller/AppStoreController.java` - API 控制器

### 前端代码
- `/frontend/src/modules/system/pages/AppStore/`
  - `index.tsx` - 应用商店页面组件
  - `index.css` - 样式文件
- `/frontend/src/services/system/appStore.ts` - API 服务层

---

## 💡 设计理念

### 为什么选择应用商店模式？
1. **灵活订阅**：企业按需购买，降低初期成本
2. **可扩展性**：新功能作为新应用上架
3. **商业化**：清晰的收费模式，利于运营
4. **租户隔离**：多租户架构，数据安全隔离

### 参考产品
- 企业微信应用市场
- 钉钉应用中心
- Shopify 应用商店
- Salesforce AppExchange

---

## ⚠️ 注意事项

1. **权限配置**：需要在数据库添加应用商店相关权限
2. **路由配置**：需要在前端路由中添加应用商店入口
3. **支付集成**：目前仅创建订单，未集成真实支付
4. **租户ID**：当前硬编码，需改为从 UserContext 获取
5. **订阅激活**：订单支付成功后需自动创建订阅记录

---

**实现完成度**：70%（数据库 ✅ + 后端 ✅ + 前端 ✅，待完成：支付 + 权限）
**预计开发工时**：支付集成 2天 + 权限控制 1天 + 测试 1天 = 4天
**商业价值**：★★★★★（核心商业化功能）
